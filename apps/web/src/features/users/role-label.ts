import type { UserRole } from '@dentalware/shared'

export const ROLE_LABEL: Record<UserRole, string> = {
  admin: 'Administrador',
  recepcion: 'Recepción',
  tecnico: 'Técnico',
  mensajero: 'Mensajero',
}
