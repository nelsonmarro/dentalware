# DataGrid modular sobre TanStack Table v9 — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sustituir `apps/web/src/components/data-table.tsx` por un `DataGrid` plug and play (`components/data-grid/`) sobre TanStack Table v9, migrar las 7 tablas y dar orden por columna real a la lista de trabajos (`orden` en la API).

**Architecture:** Núcleo headless (`useDataGrid`) que compone `tableFeatures()` a partir de una lista de módulos `GridFeature`; cada módulo aporta las features de TanStack que necesita, opciones de tabla, estado inicial y componentes para slots (toolbar, cabecera, menú de columna, pie). Compound components (`DataGrid.Root/Toolbar/Content/Pagination`) leen del contexto qué features hay y solo renderizan lo registrado. Tabla en escritorio y tarjetas en móvil desde la misma definición de columnas (`meta.mobile`). Modo servidor para trabajos: el grid emite cambios a la ruta y la ruta consulta la API.

**Tech Stack:** React 19, `@tanstack/react-table` 9.2.4, TanStack Router (URL), TanStack Query, Tailwind 4 + shadcn (`components/ui/*`), Vitest + Testing Library, Playwright. API: Hono + Drizzle (solo el parámetro `orden`).

**Spec:** `docs/superpowers/specs/2026-09-12-data-grid-design.md`. Issue #53 (tarea, Iteración 3). Contexto obligatorio: `CLAUDE.md`, `docs/conventions.md` (§5, §7), `docs/architecture.md` (§3.3, §3.5).

## Global Constraints

- **TDD** en cada tarea (regla 1): test primero, verlo fallar, mínimo código, verde, refactor. Test sin ver el rojo = tarea rechazada en revisión.
- **context7 antes de usar una API de TanStack Table v9** que no aparezca literal en este plan (regla 2). Las que sí aparecen fueron confirmadas al escribirlo: `tableFeatures`, `useTable`, `createColumnHelper<typeof features, T>()`, `helper.accessor/display/columns`, `metaHelper`, `table.FlexRender`, `rowSortingFeature`/`createSortedRowModel`/`sortFns`, `rowPaginationFeature`/`createPaginatedRowModel`, `columnFilteringFeature`/`globalFilteringFeature`/`createFilteredRowModel`/`filterFns`, `columnFacetingFeature`/`createFacetedRowModel`/`createFacetedUniqueValues`/`createFacetedMinMaxValues`, `columnGroupingFeature`/`createGroupedRowModel`/`rowExpandingFeature`/`createExpandedRowModel`/`rowAggregationFeature`/`aggregationFns`, `columnSizingFeature`/`columnResizingFeature`, `columnPinningFeature` con estado `{ start, end }` y `column.getIsPinned()` → `'start' | 'end' | false`, `column.getStart('start')`, `column.getAfter('end')`, `column.getIsLastColumn('start')`, `column.getIsFirstColumn('end')`, `header.getResizeHandler()`, `header.getSize()`, `column.getIsResizing()`, `column.resetSize()`, `column.getToggleSortingHandler()`, `column.getIsSorted()`, `column.getCanSort()`, `column.setFilterValue`, `column.getFilterValue`, `column.getFacetedUniqueValues()`, `table.setGlobalFilter`, `manualPagination`/`manualSorting`/`manualFiltering`/`rowCount`, `state` + `on*Change`, `initialState`.
- **Un componente o hook por archivo**; `kebab-case`; sin `any`; `import type` (`verbatimModuleSyntax`); sin `!` no-null salvo comentario.
- **Español** en UI y mensajes (sentence case); identificadores en inglés técnico.
- **44 px** en todo control interactivo por defecto (`Button` sin `size="sm"` salvo acciones de fila de escritorio con `pointer-coarse:` a 44, convención §5). Identificadores de fila (código, nombre) con `data-target-size="inline"`.
- **Sin scroll horizontal de página** en 1280 / 390 / 360: `overflow-x-auto` + `min-w-0` en el contenedor del grid.
- **Fronteras web** (ESLint §3.5): nada de `fetch`/`hc` fuera de `api.ts`; `data-grid/` no importa de `features/*` ni de `routes/`; los módulos `data-grid/features/*` no se importan entre sí ni desde `use-data-grid.ts`, `data-grid.tsx` o `parts/*`.
- **Tipado de TanStack**: `GridFeatures` (en `types.ts`) es el tipo del conjunto completo de features; el runtime de cada tabla solo registra las listadas en `useDataGrid`. Toda parte comprueba `grid.has('<id>')` antes de llamar a una API de esa feature. Ruling registrado en el ledger.
- **Ids de columna = claves de orden de la API** en modo servidor (en trabajos: `codigo`, `entrega`, `clinica`, `estado`). No hace falta `meta.sortKey` (desviación documentada respecto a la spec §2.2).
- **Persistencia** solo de anchos y fijadas en `localStorage` bajo `datagrid:<key>:<slice>:v1`, siempre en try/catch.
- Verificación antes de cada commit: `pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test`. E2E: `pnpm e2e:pr` al cerrar cada tabla migrada que tenga E2E; Chrome DevTools a 1280×800, 390×844 y 360×740 en cada tarea de UI (capturas en el reporte). Node 24: `export PATH=$HOME/.nvm/versions/node/v24.19.0/bin:$PATH`.
- Commits pequeños en español con `Refs #53` y los trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` y `Claude-Session: <url de la sesión>`.
- **Tres PR**: A (Tareas 1–9, rama `feat/data-grid-nucleo`), B (Tareas 10–14, rama `feat/data-grid-productos` desde A), C (Tareas 15–19, rama `feat/data-grid-trabajos` desde B, `Closes #53`). Cada PR con revisión final de rama antes de abrirse.

---

## Mapa de archivos

```
apps/web/src/components/data-grid/
  index.ts                 reexporta DataGrid, useDataGrid, defineColumns y las factorías
  types.ts                 GridFeature, GridFeatureId, GridMode, GridColumnMeta, GridFeatures, GridTable, GridColumns, GridInstance
  storage.ts               readStored / writeStored (localStorage seguro)              + storage.test.ts
  context.ts               GridContext + useGrid()
  define-columns.ts        defineColumns<T>(build)                                     + define-columns.test.tsx
  use-data-grid.ts         useDataGrid(opts) → GridInstance                             + use-data-grid.test.tsx
  data-grid.tsx            DataGrid = { Root, Toolbar, Content, Pagination }            + data-grid.test.tsx
  parts/table.tsx          tabla de escritorio (cabecera, cuerpo, filas de grupo, sticky)
  parts/header-cell.tsx    etiqueta + slots headerCell + menú de columna
  parts/column-menu.tsx    DropdownMenu con los items que aportan las features
  parts/cards.tsx          tarjetas móviles desde meta.mobile o renderCard
  parts/pagination.tsx     Anterior / Siguiente / Página N de M / conteo aria-live
  parts/toolbar.tsx        monta los slots toolbar
  parts/empty.tsx          EmptyState
  parts/pinning-styles.ts  estilos sticky por columna (usado por table.tsx)
  features/sorting.tsx     + sorting.test.tsx
  features/pagination.tsx  + pagination.test.tsx
  features/filtering.tsx   + filtering.test.tsx   (búsqueda global + filtros por columna)
  features/resizing.tsx    + resizing.test.tsx
  features/grouping.tsx    + grouping.test.tsx
  features/advanced-filter.tsx + advanced-filter.test.tsx
  features/pinning.tsx     + pinning.test.tsx
  features/url-state.ts    + url-state.test.tsx
  lib/normalize.ts         normalize() sin acentos (movido desde clinics-table)   + normalize.test.ts
apps/web/src/features/*/…-table.tsx   migradas (Tareas 7, 8, 13, 14, 18)
packages/shared/src/schemas/cases.ts  `orden` (Tarea 15)
apps/api/src/features/cases/repo.ts   orderBy por `orden` (Tarea 15)
docs/data-grid.md, CLAUDE.md, docs/conventions.md, docs/architecture.md (Tarea 19)
```

---

## Bloque A — núcleo y tablas de configuración (PR 1, rama `feat/data-grid-nucleo`)

### Task 1: Dependencia, tipos base y almacenamiento seguro

**Files:**
- Modify: `pnpm-workspace.yaml` (catalog), `apps/web/package.json` (dependencies)
- Create: `apps/web/src/components/data-grid/types.ts`
- Create: `apps/web/src/components/data-grid/storage.ts`
- Test: `apps/web/src/components/data-grid/storage.test.ts`
- Create: `apps/web/src/components/data-grid/lib/normalize.ts`
- Test: `apps/web/src/components/data-grid/lib/normalize.test.ts`

**Interfaces:**
- Produces: `GridFeatureId`, `GridMode`, `GridColumnMeta`, `GridFeatures` (tipo), `GridTable<T>`, `GridColumns<T>`, `GridFeature`, `GridSlots`, `GridInit`; `readStored<T>(key, fallback)`, `writeStored(key, value)`; `normalize(value)`.

- [ ] **Step 1: Añadir la dependencia con la versión fijada**

En `pnpm-workspace.yaml`, dentro de `catalog:` (junto a las demás `@tanstack/*`):

```yaml
  '@tanstack/react-table': 9.2.4
```

En `apps/web/package.json` → `dependencies`: `"@tanstack/react-table": "catalog:"`. Luego:

```bash
export PATH=$HOME/.nvm/versions/node/v24.19.0/bin:$PATH
pnpm install
```

Expected: `node_modules/@tanstack/react-table/package.json` con `"version": "9.2.4"`.

- [ ] **Step 2: Test de `normalize` (RED)**

`apps/web/src/components/data-grid/lib/normalize.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { normalize } from './normalize'

describe('normalize', () => {
  it('quita acentos y pasa a minúsculas', () => {
    expect(normalize('Híbrida')).toBe('hibrida')
    expect(normalize('GÓMEZ')).toBe('gomez')
  })
  it('tolera valores no textuales', () => {
    expect(normalize(12)).toBe('12')
    expect(normalize(null)).toBe('')
    expect(normalize(undefined)).toBe('')
  })
})
```

Run: `pnpm exec vitest run --project web src/components/data-grid/lib/normalize.test.ts`
Expected: FAIL — `Cannot find module './normalize'`.

- [ ] **Step 3: Implementar `normalize`**

```ts
/** Insensible a mayúsculas y acentos: «Híbrida» → «hibrida». Acepta cualquier valor de celda. */
export function normalize(value: unknown): string {
  if (value === null || value === undefined) return ''
  return String(value)
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
}
```

Run el test: PASS.

- [ ] **Step 4: Test de `storage` (RED)**

`apps/web/src/components/data-grid/storage.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'
import { readStored, writeStored } from './storage'

describe('storage del DataGrid', () => {
  afterEach(() => {
    localStorage.clear()
    vi.restoreAllMocks()
  })

  it('devuelve el valor guardado bajo la clave con versión', () => {
    writeStored('datagrid:productos:sizing', { code: 120 })
    expect(readStored('datagrid:productos:sizing', {})).toEqual({ code: 120 })
    expect(localStorage.getItem('datagrid:productos:sizing:v1')).toBe('{"code":120}')
  })

  it('devuelve el valor por defecto si no hay nada o el JSON está roto', () => {
    expect(readStored('datagrid:x:sizing', { a: 1 })).toEqual({ a: 1 })
    localStorage.setItem('datagrid:x:sizing:v1', '{rota')
    expect(readStored('datagrid:x:sizing', { a: 1 })).toEqual({ a: 1 })
  })

  it('no lanza si localStorage falla', () => {
    vi.spyOn(Storage.prototype, 'setItem').mockImplementation(() => {
      throw new Error('quota')
    })
    vi.spyOn(Storage.prototype, 'getItem').mockImplementation(() => {
      throw new Error('bloqueado')
    })
    expect(() => writeStored('datagrid:x:sizing', { a: 1 })).not.toThrow()
    expect(readStored('datagrid:x:sizing', 'fallback')).toBe('fallback')
  })
})
```

Run: FAIL — módulo inexistente.

- [ ] **Step 5: Implementar `storage.ts`**

```ts
const VERSION = 'v1'

/** Lee `${key}:v1` de localStorage; ante ausencia, JSON inválido o excepción devuelve `fallback`. */
export function readStored<T>(key: string, fallback: T): T {
  try {
    const raw = localStorage.getItem(`${key}:${VERSION}`)
    if (raw === null) return fallback
    return JSON.parse(raw) as T
  } catch {
    return fallback
  }
}

/** Escribe `${key}:v1`; nunca lanza (modo privado, cuota llena, entorno sin storage). */
export function writeStored(key: string, value: unknown): void {
  try {
    localStorage.setItem(`${key}:${VERSION}`, JSON.stringify(value))
  } catch {
    /* sin persistencia: el grid sigue funcionando con estado en memoria */
  }
}
```

Run: PASS.

- [ ] **Step 6: Escribir `types.ts`**

```ts
import type { CSSProperties, ComponentType } from 'react'
import {
  columnFacetingFeature,
  columnFilteringFeature,
  columnGroupingFeature,
  columnPinningFeature,
  columnResizingFeature,
  columnSizingFeature,
  globalFilteringFeature,
  metaHelper,
  rowAggregationFeature,
  rowExpandingFeature,
  rowPaginationFeature,
  rowSortingFeature,
  tableFeatures,
  type Column,
  type ColumnDef,
  type Header,
  type Row,
  type RowData,
  type Table,
  type TableOptions,
} from '@tanstack/react-table'

export type GridFeatureId =
  | 'pagination'
  | 'sorting'
  | 'filtering'
  | 'advancedFilter'
  | 'grouping'
  | 'resizing'
  | 'pinning'
  | 'urlState'

export type GridMode = 'client' | 'server'

/** Metadatos propios de columna: sirven a la tabla, a las tarjetas móviles y a las features. */
export type GridColumnMeta = {
  /** Control de filtro por columna (feature `filtering`). */
  filter?: 'text' | 'select' | 'range'
  /** Papel de la columna en la tarjeta móvil. Sin valor = `detail`. */
  mobile?: 'title' | 'subtitle' | 'badge' | 'detail' | 'actions' | 'hidden'
  align?: 'left' | 'right'
  /** Ancho inicial en px (feature `resizing` lo usa como `size`). */
  width?: number
  cellClassName?: string
  /** Estilo inline por fila (pestaña de color del ticket). `row` es `original` sin tipar: castear en la tabla. */
  cellStyle?: (row: unknown) => CSSProperties | undefined
  /** Agregado en filas de grupo (feature `grouping`). */
  aggregate?: 'sum' | 'count'
  /** La columna se ofrece en «Agrupar por». */
  groupable?: boolean
  /** Etiqueta para menús y filtros cuando `header` no es texto. */
  label?: string
}

/**
 * Conjunto COMPLETO de features de TanStack: solo se usa como tipo. En runtime, `useDataGrid`
 * registra únicamente las features listadas por la tabla; las partes preguntan `grid.has(id)`
 * antes de usar una API de esa feature.
 */
export const gridFeaturesForTyping = () =>
  tableFeatures({
    columnMeta: metaHelper<GridColumnMeta>(),
    columnFilteringFeature,
    globalFilteringFeature,
    columnFacetingFeature,
    rowPaginationFeature,
    rowSortingFeature,
    columnGroupingFeature,
    rowExpandingFeature,
    rowAggregationFeature,
    columnSizingFeature,
    columnResizingFeature,
    columnPinningFeature,
  })
export type GridFeatures = ReturnType<typeof gridFeaturesForTyping>

export type GridTable<T extends RowData> = Table<GridFeatures, T>
export type GridColumn<T extends RowData> = Column<GridFeatures, T, unknown>
export type GridHeader<T extends RowData> = Header<GridFeatures, T, unknown>
export type GridRow<T extends RowData> = Row<GridFeatures, T>
export type GridColumns<T extends RowData> = ColumnDef<GridFeatures, T, unknown>[]

/** Lo que una feature recibe para calcular sus opciones. */
export type GridInit = { key: string; mode: GridMode; rowCount?: number }

export type GridSlots = {
  toolbar?: ComponentType
  /** Se pinta dentro de cada `th` después de la etiqueta (botón de orden, asa de redimensionado). */
  headerCell?: ComponentType<{ header: GridHeader<never> }>
  /** Items del menú de columna (fijar, agrupar). */
  columnMenu?: ComponentType<{ column: GridColumn<never> }>
  footer?: ComponentType
}

export type GridFeature = {
  id: GridFeatureId
  /** Features, row models y fns de TanStack que este módulo necesita (se fusionan en `tableFeatures`). */
  tanstack: Record<string, unknown>
  /** Opciones de `TableOptions` (manualSorting, state + on*Change, columnResizeMode…). */
  options?: (init: GridInit) => Partial<TableOptions<GridFeatures, never>>
  /** Estado inicial que siembra (se fusiona en `initialState`). */
  initialState?: Record<string, unknown>
  slots?: GridSlots
}

export type GridInstance<T extends RowData> = {
  table: GridTable<T>
  key: string
  mode: GridMode
  features: GridFeature[]
  has: (id: GridFeatureId) => boolean
}
```

Run: `pnpm typecheck` → PASS (si `metaHelper`, `tableFeatures` o algún nombre no existe con ese nombre en 9.2.4, consultar context7 y ajustar el nombre aquí y en las tareas que lo usen; anotarlo en el reporte).

- [ ] **Step 7: Verificación y commit**

```bash
pnpm lint && pnpm format:check && pnpm typecheck && pnpm exec vitest run --project web src/components/data-grid
git add pnpm-workspace.yaml apps/web/package.json pnpm-lock.yaml apps/web/src/components/data-grid
git commit -m "feat(web): base del DataGrid: dependencia TanStack Table v9, tipos, storage seguro y normalize

Refs #53"
```

---

### Task 2: `defineColumns` y contexto

**Files:**
- Create: `apps/web/src/components/data-grid/define-columns.ts`
- Create: `apps/web/src/components/data-grid/context.ts`
- Test: `apps/web/src/components/data-grid/define-columns.test.tsx`

**Interfaces:**
- Consumes: `GridFeatures`, `GridColumns<T>`, `GridColumnMeta` (Task 1).
- Produces: `defineColumns<T>(build: (col: ColumnHelper<GridFeatures, T>) => ColumnDef[]): GridColumns<T>`; `GridContext`, `useGrid<T>()`.

- [ ] **Step 1: Test (RED)**

```tsx
import { describe, expect, it } from 'vitest'
import { defineColumns } from './define-columns'

type Row = { id: string; name: string; city: string | null }

describe('defineColumns', () => {
  it('devuelve las columnas con su meta tipado y un id por columna', () => {
    const columns = defineColumns<Row>((col) => [
      col.accessor('name', { header: 'Nombre', meta: { mobile: 'title', filter: 'text' } }),
      col.accessor('city', { header: 'Ciudad', meta: { mobile: 'subtitle' } }),
      col.display({ id: 'actions', header: '', meta: { mobile: 'actions', align: 'right' } }),
    ])
    expect(columns).toHaveLength(3)
    expect(columns[0]?.meta?.mobile).toBe('title')
    expect(columns[2]?.id).toBe('actions')
  })
})
```

Run: `pnpm exec vitest run --project web src/components/data-grid/define-columns.test.tsx` → FAIL (módulo inexistente).

- [ ] **Step 2: Implementar**

`define-columns.ts`:

```ts
import { createColumnHelper, type ColumnHelper, type RowData } from '@tanstack/react-table'
import type { GridColumns, GridFeatures } from './types'

/**
 * Define las columnas de una tabla una sola vez: sirven para la tabla de escritorio y para las
 * tarjetas móviles (`meta.mobile`). El helper viene tipado con `GridFeatures` y `GridColumnMeta`.
 */
export function defineColumns<T extends RowData>(
  build: (col: ColumnHelper<GridFeatures, T>) => GridColumns<T>,
): GridColumns<T> {
  const helper = createColumnHelper<GridFeatures, T>()
  return helper.columns(build(helper))
}
```

Si `helper.columns` no existe en 9.2.4 (context7), devolver `build(helper)` directamente.

`context.ts`:

```ts
import { createContext, useContext } from 'react'
import type { RowData } from '@tanstack/react-table'
import type { GridInstance } from './types'

export const GridContext = createContext<GridInstance<never> | null>(null)

/** Instancia del grid más cercano; falla claro si una parte se usa fuera de `DataGrid.Root`. */
export function useGrid<T extends RowData>(): GridInstance<T> {
  const grid = useContext(GridContext)
  if (!grid) throw new Error('Las partes de DataGrid deben usarse dentro de <DataGrid.Root>')
  return grid as unknown as GridInstance<T>
}
```

Run test: PASS. `pnpm typecheck`: PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): defineColumns tipado y contexto del DataGrid

Refs #53"
```

---

### Task 3: `useDataGrid`, `DataGrid.Root/Content` y partes tabla, tarjetas y vacío

**Files:**
- Create: `use-data-grid.ts`, `data-grid.tsx`, `parts/table.tsx`, `parts/header-cell.tsx`, `parts/cards.tsx`, `parts/empty.tsx`, `parts/toolbar.tsx`, `parts/pagination.tsx`, `parts/column-menu.tsx`, `parts/pinning-styles.ts`, `index.ts` (todo bajo `apps/web/src/components/data-grid/`)
- Test: `use-data-grid.test.tsx`, `data-grid.test.tsx`

**Interfaces:**
- Consumes: Task 1–2.
- Produces: `useDataGrid<T>({ key, columns, data, features?, mode?, rowCount?, getRowId })`; `DataGrid.Root({ grid, emptyMessage, emptyAction?, renderCard?, children })`, `DataGrid.Content`, `DataGrid.Toolbar`, `DataGrid.Pagination` (estos dos renderizan `null` sin su feature; se completan en Tareas 4–6).

- [ ] **Step 1: Test de `useDataGrid` sin features (RED)**

`use-data-grid.test.tsx`:

```tsx
import { renderHook } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { defineColumns } from './define-columns'
import { useDataGrid } from './use-data-grid'

type Row = { id: string; name: string }
const columns = defineColumns<Row>((col) => [col.accessor('name', { header: 'Nombre' })])
const data: Row[] = [{ id: '1', name: 'Ana' }, { id: '2', name: 'Beto' }]

describe('useDataGrid', () => {
  it('sin features expone las filas tal cual y has() es falso para todo', () => {
    const { result } = renderHook(() =>
      useDataGrid({ key: 'test', columns, data, getRowId: (r) => r.id }),
    )
    expect(result.current.table.getRowModel().rows.map((r) => r.id)).toEqual(['1', '2'])
    expect(result.current.has('sorting')).toBe(false)
    expect(result.current.mode).toBe('client')
  })

  it('fusiona las features y opciones de cada módulo registrado', () => {
    const { result } = renderHook(() =>
      useDataGrid({
        key: 'test',
        columns,
        data,
        getRowId: (r) => r.id,
        features: [{ id: 'sorting', tanstack: {}, options: () => ({ enableSorting: false }) }],
      }),
    )
    expect(result.current.has('sorting')).toBe(true)
    expect(result.current.table.options.enableSorting).toBe(false)
  })
})
```

Run: FAIL (módulo inexistente).

- [ ] **Step 2: Implementar `use-data-grid.ts`**

```ts
import { metaHelper, tableFeatures, useTable, type RowData } from '@tanstack/react-table'
import { useMemo } from 'react'
import type {
  GridColumnMeta,
  GridColumns,
  GridFeature,
  GridFeatureId,
  GridFeatures,
  GridInstance,
  GridMode,
} from './types'

export type UseDataGridOptions<T extends RowData> = {
  /** Clave estable por tabla: nombra la persistencia local (`datagrid:<key>`). */
  key: string
  columns: GridColumns<T>
  data: T[]
  features?: GridFeature[]
  mode?: GridMode
  /** Total de filas en modo servidor (para calcular páginas). */
  rowCount?: number
  getRowId: (row: T) => string
}

/**
 * Compone `tableFeatures()` y `useTable()` a partir de la lista de módulos: cada módulo aporta
 * sus features de TanStack, sus opciones y su estado inicial. Nada fuera de la lista existe en
 * runtime, aunque el tipo `GridFeatures` sea el conjunto completo.
 */
export function useDataGrid<T extends RowData>(opts: UseDataGridOptions<T>): GridInstance<T> {
  const list = opts.features ?? []
  const mode = opts.mode ?? 'client'
  const init = { key: opts.key, mode, rowCount: opts.rowCount }

  const features = useMemo(
    () =>
      tableFeatures({
        columnMeta: metaHelper<GridColumnMeta>(),
        ...Object.assign({}, ...list.map((f) => f.tanstack)),
      }) as unknown as GridFeatures,
    // La lista de features se declara una vez por tabla (constante de módulo o useMemo del llamador).
    [list],
  )

  const merged = list.reduce<Record<string, unknown>>(
    (acc, f) => ({ ...acc, ...(f.options?.(init) ?? {}) }),
    {},
  )
  const initialState = list.reduce<Record<string, unknown>>(
    (acc, f) => ({ ...acc, ...(f.initialState ?? {}) }),
    {},
  )
  const serverOptions =
    mode === 'server'
      ? { manualPagination: true, manualSorting: true, manualFiltering: true, rowCount: opts.rowCount }
      : {}

  const table = useTable<GridFeatures, T>({
    features,
    columns: opts.columns,
    data: opts.data,
    getRowId: (row) => opts.getRowId(row),
    initialState,
    ...serverOptions,
    ...merged,
  })

  const ids = new Set<GridFeatureId>(list.map((f) => f.id))
  return { table, key: opts.key, mode, features: list, has: (id) => ids.has(id) }
}
```

Nota: si `useTable` exige `initialState` con forma concreta, castear `initialState as never`. Si `getRowId` de v9 recibe `(originalRow, index, parent)`, la firma anterior sigue siendo válida.

Run test: PASS.

- [ ] **Step 3: Test de `DataGrid` (RED)** — cubre lo que hoy prueba `data-table.test.tsx` más la ausencia de controles sin features

`data-grid.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from './data-grid'
import { defineColumns } from './define-columns'
import { useDataGrid } from './use-data-grid'

type Row = { id: string; name: string; city: string; active: boolean }
const columns = defineColumns<Row>((col) => [
  col.accessor('name', { header: 'Nombre', meta: { mobile: 'title' } }),
  col.accessor('city', { header: 'Ciudad', meta: { mobile: 'subtitle' } }),
  col.accessor('active', {
    header: 'Estado',
    cell: (c) => (c.getValue() ? 'Activa' : 'Inactiva'),
    meta: { mobile: 'badge' },
  }),
])
const rows: Row[] = [
  { id: '1', name: 'Clínica Uno', city: 'Quito', active: true },
  { id: '2', name: 'Clínica Dos', city: 'Ambato', active: false },
]

function Grid({ data, action }: { data: Row[]; action?: React.ReactNode }) {
  const grid = useDataGrid({ key: 'test', columns, data, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage="No hay clínicas" emptyAction={action}>
      <DataGrid.Toolbar />
      <DataGrid.Content />
      <DataGrid.Pagination />
    </DataGrid.Root>
  )
}

describe('DataGrid', () => {
  it('en escritorio renderiza una tabla y ninguna tarjeta', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid data={rows} />)
    expect(await screen.findByRole('table')).toBeInTheDocument()
    expect(screen.getByRole('columnheader', { name: 'Nombre' })).toBeInTheDocument()
    expect(screen.getAllByRole('row')).toHaveLength(3)
    expect(screen.queryByRole('list')).not.toBeInTheDocument()
  })

  it('en móvil renderiza tarjetas con título, subtítulo y badge, y ninguna tabla', async () => {
    setMatchMedia(false)
    renderWithRouter(<Grid data={rows} />)
    expect(await screen.findByRole('list')).toBeInTheDocument()
    expect(screen.getAllByRole('listitem')).toHaveLength(2)
    expect(screen.getByText('Clínica Uno')).toBeInTheDocument()
    expect(screen.getByText('Quito')).toBeInTheDocument()
    expect(screen.getByText('Activa')).toBeInTheDocument()
    expect(screen.queryByRole('table')).not.toBeInTheDocument()
  })

  it('muestra el estado vacío con su acción', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid data={[]} action={<button>Nueva clínica</button>} />)
    expect(await screen.findByText('No hay clínicas')).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'Nueva clínica' })).toBeInTheDocument()
    expect(screen.queryByRole('table')).not.toBeInTheDocument()
  })

  it('sin features no hay barra, ni botones de orden, ni paginación', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid data={rows} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('button')).not.toBeInTheDocument()
    expect(screen.queryByRole('navigation', { name: 'Paginación' })).not.toBeInTheDocument()
    expect(screen.queryByRole('search')).not.toBeInTheDocument()
  })
})
```

Run: FAIL (módulo inexistente).

- [ ] **Step 4: Implementar `data-grid.tsx` y las partes**

`data-grid.tsx`:

```tsx
import type { ReactNode } from 'react'
import type { RowData } from '@tanstack/react-table'
import { useMediaQuery } from '@/lib/use-media-query'
import { GridContext } from './context'
import { GridCards } from './parts/cards'
import { GridEmpty } from './parts/empty'
import { GridPagination } from './parts/pagination'
import { GridTable } from './parts/table'
import { GridToolbar } from './parts/toolbar'
import type { GridInstance } from './types'

// Tailwind `lg` empieza en 1024px; debe coincidir con las clases `lg:*` de las partes.
const DESKTOP_QUERY = '(min-width: 1024px)'

type RootProps<T extends RowData> = {
  grid: GridInstance<T>
  emptyMessage: string
  emptyAction?: ReactNode
  /** Tarjeta móvil a medida; sin ella se genera desde `meta.mobile`. */
  renderCard?: (row: T) => ReactNode
  children: ReactNode
}

type RootContextValue = { emptyMessage: string; emptyAction?: ReactNode; renderCard?: (row: never) => ReactNode }
export const RootPropsContext = createContext<RootContextValue | null>(null)

function Root<T extends RowData>({ grid, emptyMessage, emptyAction, renderCard, children }: RootProps<T>) {
  return (
    <GridContext.Provider value={grid as unknown as GridInstance<never>}>
      <RootPropsContext.Provider
        value={{ emptyMessage, emptyAction, renderCard: renderCard as RootContextValue['renderCard'] }}
      >
        <div className="flex min-w-0 flex-col gap-3">{children}</div>
      </RootPropsContext.Provider>
    </GridContext.Provider>
  )
}

/** Tabla en escritorio, tarjetas en móvil (una sola variante montada), vacío cuando no hay filas. */
function Content() {
  const grid = useGrid()
  const props = useRootProps()
  const isDesktop = useMediaQuery(DESKTOP_QUERY)
  if (grid.table.getRowModel().rows.length === 0)
    return <GridEmpty message={props.emptyMessage} action={props.emptyAction} />
  return isDesktop ? <GridTable /> : <GridCards />
}

export const DataGrid = { Root, Content, Toolbar: GridToolbar, Pagination: GridPagination }
```

(Añadir `import { createContext, useContext } from 'react'`, `useGrid` de `./context` y un `useRootProps()` que lea `RootPropsContext` y lance si falta, en el mismo archivo.)

`parts/empty.tsx`:

```tsx
import type { ReactNode } from 'react'
import { EmptyState } from '@/components/empty-state'

export function GridEmpty({ message, action }: { message: string; action?: ReactNode }) {
  return <EmptyState title={message} action={action} />
}
```

`parts/toolbar.tsx` (base; las features añaden slots en Tareas 6, 11, 12):

```tsx
import { useGrid } from '../context'

/** Monta los slots `toolbar` de las features registradas, en el orden de registro. Sin slots no renderiza nada. */
export function GridToolbar() {
  const grid = useGrid()
  const slots = grid.features.flatMap((f) => (f.slots?.toolbar ? [{ id: f.id, Slot: f.slots.toolbar }] : []))
  if (slots.length === 0) return null
  return (
    <div role="search" className="flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-end">
      {slots.map(({ id, Slot }) => (
        <Slot key={id} />
      ))}
    </div>
  )
}
```

`parts/pagination.tsx` (base; se completa en Task 5):

```tsx
import { useGrid } from '../context'

export function GridPagination() {
  const grid = useGrid()
  if (!grid.has('pagination')) return null
  return null // Task 5 implementa los controles
}
```

`parts/pinning-styles.ts` (base; Task 16 la completa):

```ts
import type { CSSProperties } from 'react'
import type { GridColumn } from '../types'

/** Estilos sticky para columnas fijadas. Sin feature `pinning` devuelve `{}`. */
export function pinningStyles(_column: GridColumn<never>, hasPinning: boolean): CSSProperties {
  if (!hasPinning) return {}
  return {}
}
```

`parts/column-menu.tsx` (base; Tareas 11 y 16 aportan items):

```tsx
import { MoreVertical } from 'lucide-react'
import { useGrid } from '../context'
import { Button } from '@/components/ui/button'
import { DropdownMenu, DropdownMenuContent, DropdownMenuTrigger } from '@/components/ui/dropdown-menu'
import type { GridColumn } from '../types'

/** Menú de columna: solo aparece si alguna feature registró items (`slots.columnMenu`). */
export function ColumnMenu({ column, label }: { column: GridColumn<never>; label: string }) {
  const grid = useGrid()
  const items = grid.features.flatMap((f) => (f.slots?.columnMenu ? [{ id: f.id, Item: f.slots.columnMenu }] : []))
  if (items.length === 0) return null
  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon-sm" aria-label={`Opciones de la columna ${label}`}>
          <MoreVertical className="size-4" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        {items.map(({ id, Item }) => (
          <Item key={id} column={column} />
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  )
}
```

`parts/header-cell.tsx`:

```tsx
import { useGrid } from '../context'
import type { GridHeader } from '../types'
import { ColumnMenu } from './column-menu'

/** Etiqueta de la columna + slots `headerCell` de las features (orden, asa) + menú de columna. */
export function HeaderCell({ header }: { header: GridHeader<never> }) {
  const grid = useGrid()
  const meta = header.column.columnDef.meta
  const label =
    meta?.label ?? (typeof header.column.columnDef.header === 'string' ? header.column.columnDef.header : header.column.id)
  const slots = grid.features.flatMap((f) => (f.slots?.headerCell ? [{ id: f.id, Slot: f.slots.headerCell }] : []))
  return (
    <div className={`flex items-center gap-1 ${meta?.align === 'right' ? 'justify-end' : ''}`}>
      {header.isPlaceholder ? null : <grid.table.FlexRender header={header} />}
      {slots.map(({ id, Slot }) => (
        <Slot key={id} header={header} />
      ))}
      <ColumnMenu column={header.column} label={label} />
    </div>
  )
}
```

`parts/table.tsx`:

```tsx
import { useGrid } from '../context'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { HeaderCell } from './header-cell'
import { pinningStyles } from './pinning-styles'

export function GridTable() {
  const grid = useGrid()
  const { table } = grid
  const hasPinning = grid.has('pinning')
  const hasResizing = grid.has('resizing')
  return (
    <div className="min-w-0 overflow-x-auto rounded-xl border border-border bg-card">
      <Table className="[&_tr>*:last-child]:pr-5" style={hasResizing ? { width: table.getTotalSize() } : undefined}>
        <TableHeader>
          {table.getHeaderGroups().map((group) => (
            <TableRow key={group.id}>
              {group.headers.map((header) => {
                const meta = header.column.columnDef.meta
                const isSorted = grid.has('sorting') ? header.column.getIsSorted() : false
                return (
                  <TableHead
                    key={header.id}
                    aria-sort={isSorted === 'asc' ? 'ascending' : isSorted === 'desc' ? 'descending' : undefined}
                    className={`${meta?.cellClassName ?? ''} ${meta?.align === 'right' ? 'text-right' : ''}`}
                    style={{
                      ...pinningStyles(header.column, hasPinning),
                      ...(hasResizing ? { width: header.getSize() } : {}),
                    }}
                  >
                    <HeaderCell header={header} />
                  </TableHead>
                )
              })}
            </TableRow>
          ))}
        </TableHeader>
        <TableBody>
          {table.getRowModel().rows.map((row) => (
            <TableRow key={row.id} data-grouped={grid.has('grouping') && row.getIsGrouped() ? '' : undefined}>
              {row.getAllCells().map((cell) => {
                const meta = cell.column.columnDef.meta
                return (
                  <TableCell
                    key={cell.id}
                    className={`${meta?.cellClassName ?? ''} ${meta?.align === 'right' ? 'text-right' : ''}`}
                    style={{ ...pinningStyles(cell.column, hasPinning), ...meta?.cellStyle?.(row.original) }}
                  >
                    <table.FlexRender cell={cell} />
                  </TableCell>
                )
              })}
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </div>
  )
}
```

(`table.getTotalSize()` solo se llama con `resizing`; si el nombre difiere en 9.2.4, context7.) Las filas de grupo se completan en Task 11.

`parts/cards.tsx`:

```tsx
import { useGrid } from '../context'
import { useRootProps } from '../data-grid'

/** Tarjetas móviles: `renderCard` si existe; si no, se generan desde `meta.mobile`. */
export function GridCards() {
  const grid = useGrid()
  const { renderCard } = useRootProps()
  const rows = grid.table.getRowModel().rows
  return (
    <ul className="flex flex-col gap-3">
      {rows.map((row) => (
        <li key={row.id} className="rounded-xl border border-border bg-card p-4">
          {renderCard ? renderCard(row.original as never) : <AutoCard row={row} />}
        </li>
      ))}
    </ul>
  )
}

function AutoCard({ row }: { row: ReturnType<ReturnType<typeof useGrid>['table']['getRowModel']>['rows'][number] }) {
  const grid = useGrid()
  const cells = row.getAllCells()
  const byRole = (role: string) => cells.filter((c) => (c.column.columnDef.meta?.mobile ?? 'detail') === role)
  const render = (c: (typeof cells)[number]) => <grid.table.FlexRender key={c.id} cell={c} />
  return (
    <div className="flex flex-col gap-2">
      <div className="flex items-center justify-between gap-2">
        <span className="font-medium">{byRole('title').map(render)}</span>
        <span>{byRole('badge').map(render)}</span>
      </div>
      {byRole('subtitle').length > 0 && (
        <p className="text-sm text-muted-foreground">{byRole('subtitle').map(render)}</p>
      )}
      {byRole('detail').map((c) => (
        <p key={c.id} className="text-sm">
          <span className="text-muted-foreground">
            {typeof c.column.columnDef.header === 'string' ? c.column.columnDef.header : c.column.id}:{' '}
          </span>
          {render(c)}
        </p>
      ))}
      {byRole('actions').map(render)}
    </div>
  )
}
```

`index.ts`:

```ts
export { DataGrid } from './data-grid'
export { defineColumns } from './define-columns'
export { useDataGrid } from './use-data-grid'
export type { GridColumnMeta, GridColumns, GridFeature, GridInstance, GridMode } from './types'
```

Run `data-grid.test.tsx` y `use-data-grid.test.tsx`: PASS. `pnpm lint && pnpm typecheck`: PASS (la regla de `Button size="icon-sm"`: confirmar que existe en `button.tsx`; si no, usar `size="icon"`).

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): núcleo del DataGrid: useDataGrid, Root/Content, tabla, tarjetas y vacío sin features

Refs #53"
```

---

### Task 4: Feature `sorting`

**Files:**
- Create: `apps/web/src/components/data-grid/features/sorting.tsx`
- Test: `apps/web/src/components/data-grid/features/sorting.test.tsx`
- Modify: `index.ts` (exportar `sorting`)

**Interfaces:**
- Produces: `sorting(opts?: { multi?: boolean }): GridFeature` con slot `headerCell` (botón «Ordenar por X»).

- [ ] **Step 1: Test (RED)**

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from '../data-grid'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { sorting } from './sorting'

type Row = { id: string; name: string; days: number }
const columns = defineColumns<Row>((col) => [
  col.accessor('name', { header: 'Nombre' }),
  col.accessor('days', { header: 'Días' }),
])
const rows: Row[] = [
  { id: '1', name: 'Zirconio', days: 5 },
  { id: '2', name: 'Acrílico', days: 12 },
]
const FEATURES = [sorting()]

function Grid({ features }: { features: typeof FEATURES | [] }) {
  const grid = useDataGrid({ key: 'test', columns, data: rows, features, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage="Vacío">
      <DataGrid.Content />
    </DataGrid.Root>
  )
}
const cellsOf = (col: number) =>
  screen.getAllByRole('row').slice(1).map((r) => r.querySelectorAll('td')[col]?.textContent)

describe('feature sorting', () => {
  it('ordena al pulsar la cabecera y anuncia aria-sort', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={FEATURES} />)
    const button = await screen.findByRole('button', { name: 'Ordenar por Nombre' })
    await user.click(button)
    expect(cellsOf(0)).toEqual(['Acrílico', 'Zirconio'])
    expect(screen.getByRole('columnheader', { name: /Nombre/ })).toHaveAttribute('aria-sort', 'ascending')
    await user.click(button)
    expect(cellsOf(0)).toEqual(['Zirconio', 'Acrílico'])
    expect(screen.getByRole('columnheader', { name: /Nombre/ })).toHaveAttribute('aria-sort', 'descending')
  })

  it('sin la feature no hay botones de orden', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[]} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('button', { name: /Ordenar por/ })).not.toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: Implementar `features/sorting.tsx`**

```tsx
import { createSortedRowModel, rowSortingFeature, sortFns } from '@tanstack/react-table'
import { ArrowDown, ArrowUp, ArrowUpDown } from 'lucide-react'
import { Button } from '@/components/ui/button'
import type { GridFeature, GridHeader } from '../types'

function SortButton({ header }: { header: GridHeader<never> }) {
  const column = header.column
  if (!column.getCanSort()) return null
  const sorted = column.getIsSorted()
  const label =
    column.columnDef.meta?.label ?? (typeof column.columnDef.header === 'string' ? column.columnDef.header : column.id)
  const Icon = sorted === 'asc' ? ArrowUp : sorted === 'desc' ? ArrowDown : ArrowUpDown
  return (
    <Button
      type="button"
      variant="ghost"
      size="icon-sm"
      aria-label={`Ordenar por ${label}`}
      onClick={column.getToggleSortingHandler()}
    >
      <Icon aria-hidden className="size-4" />
    </Button>
  )
}

/** Orden por columna (cliente o servidor). `multi` habilita varias columnas con Shift. */
export function sorting(opts: { multi?: boolean } = {}): GridFeature {
  return {
    id: 'sorting',
    tanstack: { rowSortingFeature, sortedRowModel: createSortedRowModel(), sortFns },
    options: () => ({ enableMultiSort: opts.multi ?? false }),
    slots: { headerCell: SortButton },
  }
}
```

Run: PASS (si `getToggleSortingHandler` devuelve `undefined` cuando no se puede ordenar, ya está cubierto por `getCanSort`).

- [ ] **Step 3: Exportar en `index.ts` y commit**

```ts
export { sorting } from './features/sorting'
```

```bash
pnpm lint && pnpm typecheck && pnpm exec vitest run --project web src/components/data-grid
git add apps/web/src/components/data-grid
git commit -m "feat(web): feature de orden del DataGrid con aria-sort

Refs #53"
```

---

### Task 5: Feature `pagination` (cliente y servidor)

**Files:**
- Create: `features/pagination.tsx`; Modify: `parts/pagination.tsx`, `index.ts`
- Test: `features/pagination.test.tsx`

**Interfaces:**
- Produces: `pagination(opts?: { pageSize?: number }): GridFeature` (por defecto 25). `DataGrid.Pagination` con `nav aria-label="Paginación"`, botones «Anterior»/«Siguiente» (h-11), texto «Página N de M» y conteo `aria-live`.

- [ ] **Step 1: Test (RED)**

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from '../data-grid'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { pagination } from './pagination'

type Row = { id: string; name: string }
const columns = defineColumns<Row>((col) => [col.accessor('name', { header: 'Nombre' })])
const rows: Row[] = Array.from({ length: 7 }, (_, i) => ({ id: String(i), name: `Fila ${i}` }))

function Grid({ features, mode, rowCount }: { features: ReturnType<typeof pagination>[]; mode?: 'client' | 'server'; rowCount?: number }) {
  const grid = useDataGrid({ key: 'test', columns, data: rows, features, mode, rowCount, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage="Vacío">
      <DataGrid.Content />
      <DataGrid.Pagination />
    </DataGrid.Root>
  )
}

describe('feature pagination', () => {
  it('en cliente muestra 3 por página y avanza', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={[pagination({ pageSize: 3 })]} />)
    await screen.findByRole('navigation', { name: 'Paginación' })
    expect(screen.getAllByRole('row')).toHaveLength(4)
    expect(screen.getByText('Página 1 de 3')).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'Anterior' })).toBeDisabled()
    await user.click(screen.getByRole('button', { name: 'Siguiente' }))
    expect(screen.getByText('Página 2 de 3')).toBeInTheDocument()
    expect(screen.getByText('Fila 3')).toBeInTheDocument()
  })

  it('en servidor no corta las filas y calcula las páginas con rowCount', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[pagination({ pageSize: 3 })]} mode="server" rowCount={30} />)
    await screen.findByRole('navigation', { name: 'Paginación' })
    expect(screen.getAllByRole('row')).toHaveLength(8) // las 7 filas que llegaron + cabecera
    expect(screen.getByText('Página 1 de 10')).toBeInTheDocument()
  })

  it('sin la feature no hay paginación', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[]} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('navigation', { name: 'Paginación' })).not.toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: Implementar `features/pagination.tsx`**

```tsx
import { createPaginatedRowModel, rowPaginationFeature } from '@tanstack/react-table'
import type { GridFeature } from '../types'

export const DEFAULT_PAGE_SIZE = 25

/** Paginación: cliente (row model paginado) o servidor (`manualPagination` + `rowCount`, ver useDataGrid). */
export function pagination(opts: { pageSize?: number } = {}): GridFeature {
  const pageSize = opts.pageSize ?? DEFAULT_PAGE_SIZE
  return {
    id: 'pagination',
    tanstack: { rowPaginationFeature, paginatedRowModel: createPaginatedRowModel() },
    initialState: { pagination: { pageIndex: 0, pageSize } },
    options: (init) => ({ autoResetPageIndex: init.mode === 'client' }),
  }
}
```

`parts/pagination.tsx`:

```tsx
import { Button } from '@/components/ui/button'
import { useGrid } from '../context'

export function GridPagination() {
  const grid = useGrid()
  if (!grid.has('pagination')) return null
  const { table } = grid
  const { pageIndex, pageSize } = table.getState().pagination
  const pageCount = Math.max(table.getPageCount(), 1)
  const total = grid.mode === 'server' ? (table.options.rowCount ?? 0) : table.getPrePaginationRowModel().rows.length
  const from = total === 0 ? 0 : pageIndex * pageSize + 1
  const to = Math.min((pageIndex + 1) * pageSize, total)
  return (
    <nav aria-label="Paginación" className="flex items-center justify-between gap-3">
      <Button type="button" variant="outline" className="h-11" disabled={!table.getCanPreviousPage()} onClick={() => table.previousPage()}>
        Anterior
      </Button>
      <p className="text-sm text-muted-foreground">
        <span>Página {pageIndex + 1} de {pageCount}</span>
        <span aria-live="polite" className="sr-only">
          Mostrando {from} a {to} de {total}
        </span>
      </p>
      <Button type="button" variant="outline" className="h-11" disabled={!table.getCanNextPage()} onClick={() => table.nextPage()}>
        Siguiente
      </Button>
    </nav>
  )
}
```

Si `table.getState()` no existe en v9 (el estado se lee con `table.state` tras un selector, o con `table.store`), usar `useTable(opts, (s) => s)` en `useDataGrid` (selector identidad) y leer `table.state.pagination`; confirmar con context7 y anotar en el reporte.

Run: PASS.

- [ ] **Step 3: Exportar y commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): paginación del DataGrid en cliente y servidor con conteo accesible

Refs #53"
```

---

### Task 6: Feature `filtering` (búsqueda global + filtros por columna)

**Files:**
- Create: `features/filtering.tsx`; Modify: `index.ts`
- Test: `features/filtering.test.tsx`

**Interfaces:**
- Produces: `filtering(opts: { search?: { label: string; placeholder?: string; id: string }; columns?: boolean }): GridFeature` con slot `toolbar` (buscador con `<Label>` visible + controles por columna según `meta.filter`). Mensaje vacío con búsqueda: el llamador lo compone (`emptyMessage` recibe función en Root: ver Step 2).

- [ ] **Step 1: Test (RED)**

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from '../data-grid'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { filtering } from './filtering'

type Row = { id: string; name: string; category: string; days: number }
const columns = defineColumns<Row>((col) => [
  col.accessor('name', { header: 'Nombre', meta: { filter: 'text' } }),
  col.accessor('category', { header: 'Categoría', meta: { filter: 'select' } }),
  col.accessor('days', { header: 'Días', meta: { filter: 'range' } }),
])
const rows: Row[] = [
  { id: '1', name: 'Prótesis híbrida', category: 'Removible', days: 12 },
  { id: '2', name: 'Zirconio', category: 'Fija', days: 5 },
]
const withSearch = [filtering({ search: { id: 'buscar', label: 'Buscar producto' } })]
const withColumns = [filtering({ columns: true })]

function Grid({ features }: { features: ReturnType<typeof filtering>[] }) {
  const grid = useDataGrid({ key: 'test', columns, data: rows, features, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage={(q) => (q ? `Ningún producto coincide con "${q}"` : 'No hay productos')}>
      <DataGrid.Toolbar />
      <DataGrid.Content />
    </DataGrid.Root>
  )
}

describe('feature filtering', () => {
  it('el buscador tiene etiqueta visible y filtra sin acentos', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={withSearch} />)
    await user.type(await screen.findByLabelText('Buscar producto'), 'hibrida')
    expect(screen.getByText('Prótesis híbrida')).toBeInTheDocument()
    expect(screen.queryByText('Zirconio')).not.toBeInTheDocument()
  })

  it('muestra el vacío con el texto buscado', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={withSearch} />)
    await user.type(await screen.findByLabelText('Buscar producto'), 'no existe')
    expect(await screen.findByText('Ningún producto coincide con "no existe"')).toBeInTheDocument()
  })

  it('filtra por columna select con los valores únicos y por rango', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={withColumns} />)
    await user.selectOptions(await screen.findByLabelText('Filtrar Categoría'), 'Fija')
    expect(screen.queryByText('Prótesis híbrida')).not.toBeInTheDocument()
    await user.selectOptions(screen.getByLabelText('Filtrar Categoría'), '')
    await user.type(screen.getByLabelText('Días mínimo'), '10')
    expect(screen.queryByText('Zirconio')).not.toBeInTheDocument()
    expect(screen.getByText('Prótesis híbrida')).toBeInTheDocument()
  })

  it('sin la feature no hay buscador ni filtros', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[]} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('search')).not.toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: Ampliar `DataGrid.Root` para `emptyMessage` como función**

En `data-grid.tsx`: `emptyMessage: string | ((query: string) => string)`. En `Content`, calcular `const query = grid.has('filtering') ? String(grid.table.getState().globalFilter ?? '') : ''` y `const message = typeof props.emptyMessage === 'function' ? props.emptyMessage(query.trim()) : props.emptyMessage`. Además, con búsqueda activa no se muestra `emptyAction` (igual que hoy en clínicas): `action={query.trim() ? undefined : props.emptyAction}`.

- [ ] **Step 3: Implementar `features/filtering.tsx`**

```tsx
import {
  columnFacetingFeature,
  columnFilteringFeature,
  createFacetedMinMaxValues,
  createFacetedRowModel,
  createFacetedUniqueValues,
  createFilteredRowModel,
  filterFns,
  globalFilteringFeature,
} from '@tanstack/react-table'
import { useGrid } from '../context'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { normalize } from '../lib/normalize'
import type { GridColumn, GridFeature } from '../types'

/** Filtro de texto insensible a acentos y mayúsculas (celda y valor buscado normalizados). */
const sinAcentos = (row: { getValue: (id: string) => unknown }, columnId: string, value: unknown) =>
  normalize(row.getValue(columnId)).includes(normalize(value))

type SearchOpts = { id: string; label: string; placeholder?: string }

function Toolbar({ search, columns }: { search?: SearchOpts; columns: boolean }) {
  const grid = useGrid()
  const { table } = grid
  const query = String(table.getState().globalFilter ?? '')
  const filterable = columns ? table.getAllLeafColumns().filter((c) => c.columnDef.meta?.filter) : []
  return (
    <>
      {search && (
        <div className="flex flex-col gap-1.5 sm:max-w-xs">
          <Label htmlFor={search.id}>{search.label}</Label>
          <Input
            id={search.id}
            type="search"
            placeholder={search.placeholder}
            className="h-11"
            value={query}
            onChange={(e) => table.setGlobalFilter(e.target.value)}
          />
        </div>
      )}
      {filterable.map((column) => (
        <ColumnFilter key={column.id} column={column} />
      ))}
    </>
  )
}

function labelOf(column: GridColumn<never>) {
  return column.columnDef.meta?.label ?? (typeof column.columnDef.header === 'string' ? column.columnDef.header : column.id)
}

function ColumnFilter({ column }: { column: GridColumn<never> }) {
  const kind = column.columnDef.meta?.filter
  const label = labelOf(column)
  const value = column.getFilterValue()
  if (kind === 'select') {
    const options = Array.from(column.getFacetedUniqueValues().keys()).map(String).sort()
    return (
      <div className="flex flex-col gap-1.5">
        <Label htmlFor={`filtro-${column.id}`}>Filtrar {label}</Label>
        <select
          id={`filtro-${column.id}`}
          className="h-11 rounded-md border border-input bg-background px-3 text-sm"
          value={typeof value === 'string' ? value : ''}
          onChange={(e) => column.setFilterValue(e.target.value || undefined)}
        >
          <option value="">Todas</option>
          {options.map((o) => (
            <option key={o} value={o}>
              {o}
            </option>
          ))}
        </select>
      </div>
    )
  }
  if (kind === 'range') {
    const [min, max] = (Array.isArray(value) ? value : [undefined, undefined]) as [number | undefined, number | undefined]
    const set = (next: [number | undefined, number | undefined]) =>
      column.setFilterValue(next[0] === undefined && next[1] === undefined ? undefined : next)
    return (
      <div className="flex items-end gap-2">
        <div className="flex flex-col gap-1.5">
          <Label htmlFor={`min-${column.id}`}>{label} mínimo</Label>
          <Input id={`min-${column.id}`} inputMode="numeric" className="h-11 w-24" value={min ?? ''}
            onChange={(e) => set([e.target.value === '' ? undefined : Number(e.target.value), max])} />
        </div>
        <div className="flex flex-col gap-1.5">
          <Label htmlFor={`max-${column.id}`}>{label} máximo</Label>
          <Input id={`max-${column.id}`} inputMode="numeric" className="h-11 w-24" value={max ?? ''}
            onChange={(e) => set([min, e.target.value === '' ? undefined : Number(e.target.value)])} />
        </div>
      </div>
    )
  }
  return (
    <div className="flex flex-col gap-1.5">
      <Label htmlFor={`filtro-${column.id}`}>Filtrar {label}</Label>
      <Input id={`filtro-${column.id}`} type="search" className="h-11" value={typeof value === 'string' ? value : ''}
        onChange={(e) => column.setFilterValue(e.target.value || undefined)} />
    </div>
  )
}

/**
 * Búsqueda global (con `<label>` visible, UX1-09) y/o filtros por columna según `meta.filter`.
 * Texto: sin acentos; select: valores únicos (faceted); range: `[min, max]` con `inNumberRange`.
 */
export function filtering(opts: { search?: SearchOpts; columns?: boolean } = {}): GridFeature {
  const columns = opts.columns ?? false
  return {
    id: 'filtering',
    tanstack: {
      columnFilteringFeature,
      globalFilteringFeature,
      columnFacetingFeature,
      filteredRowModel: createFilteredRowModel(),
      facetedRowModel: createFacetedRowModel(),
      facetedUniqueValues: createFacetedUniqueValues(),
      facetedMinMaxValues: createFacetedMinMaxValues(),
      filterFns: { ...filterFns, sinAcentos },
    },
    options: () => ({
      globalFilterFn: sinAcentos,
      // Cada columna con meta.filter 'text' usa sinAcentos; 'select' igualdad; 'range' inNumberRange.
      defaultColumn: { filterFn: 'sinAcentos' },
    }),
    initialState: { globalFilter: '' },
    slots: { toolbar: () => <Toolbar search={opts.search} columns={columns} /> },
  }
}
```

Para `select` y `range`, el `filterFn` se fija por columna en `useDataGrid`? No: más simple, en `filtering()` no se toca; en `defineColumns` el llamador no debe repetirlo. Solución: `options` devuelve `defaultColumn: { filterFn: 'sinAcentos' }` y la feature envuelve `sinAcentos` para que, si el valor es un array, aplique rango numérico, y si la columna tiene `meta.filter === 'select'`, igualdad exacta:

```ts
const gridFilter = (row, columnId, value, addMeta?) => {
  if (Array.isArray(value)) {
    const n = Number(row.getValue(columnId)); const [min, max] = value as [number?, number?]
    return (min === undefined || n >= min) && (max === undefined || n <= max)
  }
  return sinAcentos(row, columnId, value)
}
```

y para `select`, el toolbar compara con `normalize` igualmente (igualdad tras normalizar: `normalize(cell) === normalize(value)`); implementarlo dentro de `gridFilter` consultando `column.columnDef.meta?.filter` vía `row.table.getColumn(columnId)`. Registrar `filterFns: { ...filterFns, gridFilter }` y `defaultColumn: { filterFn: 'gridFilter' }`. Ajustar nombres si context7 indica otra firma para `FilterFn` en v9.

Run: PASS.

- [ ] **Step 4: Exportar y commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): filtros del DataGrid: búsqueda global con etiqueta y filtros por columna

Refs #53"
```

---

### Task 7: Migrar clínicas y doctores

**Files:**
- Modify: `apps/web/src/features/clinics/clinics-table.tsx`, `apps/web/src/features/doctors/doctors-table.tsx`
- Test: `apps/web/src/features/clinics/clinics-table.test.tsx` (existente + 2 casos), crear `apps/web/src/features/doctors/doctors-table.test.tsx`

**Interfaces:**
- Consumes: `DataGrid`, `useDataGrid`, `defineColumns`, `sorting`, `pagination`, `filtering` de `@/components/data-grid`.
- Produces: mismas props públicas que hoy (`clinics/onEdit/onToggle/emptyAction`; `doctors/onEdit/onToggle/emptyAction`).

- [ ] **Step 1: Añadir tests nuevos a `clinics-table.test.tsx` (RED)**

```tsx
  it('ordena por nombre al pulsar la cabecera', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<ClinicsTable clinics={CLINICS} onEdit={vi.fn()} onToggle={vi.fn()} />)
    await user.click(await screen.findByRole('button', { name: 'Ordenar por Clínica' }))
    const names = screen.getAllByRole('link').map((l) => l.textContent)
    expect(names).toEqual(['Clínica Uno', 'Zirconio Dental'])
  })

  it('pagina de 25 en 25', async () => {
    setMatchMedia(true)
    const many = Array.from({ length: 26 }, (_, i) => ({ ...CLINICS[0]!, id: `c${i}`, name: `Clínica ${String(i).padStart(2, '0')}` }))
    renderWithRouter(<ClinicsTable clinics={many} onEdit={vi.fn()} onToggle={vi.fn()} />)
    await screen.findByRole('navigation', { name: 'Paginación' })
    expect(screen.getAllByRole('link')).toHaveLength(25)
    expect(screen.getByText('Página 1 de 2')).toBeInTheDocument()
  })
```

Run: FAIL (no hay botón de orden ni paginación).

- [ ] **Step 2: Reescribir `clinics-table.tsx` sobre `DataGrid`**

```tsx
import { Link } from '@tanstack/react-router'
import { Pencil } from 'lucide-react'
import { useMemo } from 'react'
import { ActiveBadge } from '@/components/active-badge'
import { DataGrid, defineColumns, filtering, pagination, sorting, useDataGrid } from '@/components/data-grid'
import { Button } from '@/components/ui/button'
import { Switch } from '@/components/ui/switch'
import type { Clinic } from './api'

const FEATURES = [
  filtering({ search: { id: 'clinicas-buscar', label: 'Buscar clínica', placeholder: 'Buscar por nombre' } }),
  sorting(),
  pagination(),
]

export function ClinicsTable({ clinics, onEdit, onToggle, emptyAction }: {
  clinics: Clinic[]
  onEdit: (c: Clinic) => void
  onToggle: (c: Clinic, active: boolean) => void
  emptyAction?: React.ReactNode
}) {
  const columns = useMemo(
    () =>
      defineColumns<Clinic>((col) => [
        col.accessor('name', {
          header: 'Clínica',
          meta: { mobile: 'title' },
          // `data-target-size="inline"`: identificador de fila, no una acción (WCAG 2.5.8).
          cell: (c) => (
            <Link to="/configuracion/clinicas/$clinicId" params={{ clinicId: c.row.original.id }}
              data-target-size="inline" className="font-medium text-primary hover:underline">
              {c.getValue()}
            </Link>
          ),
        }),
        col.accessor('city', { header: 'Ciudad', cell: (c) => c.getValue() ?? '—', meta: { mobile: 'subtitle' } }),
        col.accessor('whatsapp', { header: 'WhatsApp', cell: (c) => <span className="font-mono text-sm">{c.getValue() ?? '—'}</span>, meta: { mobile: 'subtitle' } }),
        col.accessor('paymentTermsDays', { header: 'Crédito', cell: (c) => `${c.getValue()} días`, meta: { mobile: 'hidden' } }),
        col.accessor('active', { header: 'Estado', cell: (c) => <ActiveBadge active={c.getValue()} />, meta: { mobile: 'badge' }, enableGlobalFilter: false }),
        col.display({
          id: 'actions',
          header: '',
          meta: { mobile: 'actions', align: 'right' },
          cell: (c) => {
            const clinic = c.row.original
            return (
              <div className="flex items-center justify-end gap-3">
                <Button variant="ghost" size="icon" aria-label={`Editar ${clinic.name}`} onClick={() => onEdit(clinic)}>
                  <Pencil className="size-4" />
                </Button>
                <Switch checked={clinic.active} aria-label={`${clinic.name} activa`} onCheckedChange={(v) => onToggle(clinic, v)} />
              </div>
            )
          },
        }),
      ]),
    [onEdit, onToggle],
  )
  const grid = useDataGrid({ key: 'clinicas', columns, data: clinics, features: FEATURES, getRowId: (c) => c.id })
  return (
    <DataGrid.Root
      grid={grid}
      emptyMessage={(q) => (q ? `Ninguna clínica coincide con "${q}"` : 'Aún no hay clínicas. Crea la primera con Nueva clínica.')}
      emptyAction={emptyAction}
    >
      <DataGrid.Toolbar />
      <DataGrid.Content />
      <DataGrid.Pagination />
    </DataGrid.Root>
  )
}
```

La búsqueda global debe coincidir solo por nombre como hoy: poner `enableGlobalFilter: false` en `city`, `whatsapp`, `paymentTermsDays` (y `active`). Confirmar en context7 el nombre de la opción de columna (`enableGlobalFilter`).

Run `clinics-table.test.tsx` completo: PASS (los 3 existentes sin cambios + 2 nuevos).

- [ ] **Step 3: Test de doctores (RED) y migración**

`doctors-table.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it, vi } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import type { Doctor } from './api'
import { DoctorsTable } from './doctors-table'

const DOCTORS = [
  { id: 'd1', name: 'Dra. Ruiz', phone: null, email: null, active: true },
  { id: 'd2', name: 'Dr. Gómez', phone: '0991234567', email: 'g@x.com', active: false },
] as unknown as Doctor[]

describe('DoctorsTable', () => {
  it('ordena por nombre y busca sin acentos', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<DoctorsTable doctors={DOCTORS} onEdit={vi.fn()} onToggle={vi.fn()} />)
    await user.click(await screen.findByRole('button', { name: 'Ordenar por Doctor' }))
    const cells = screen.getAllByRole('row').slice(1).map((r) => r.querySelector('td')?.textContent)
    expect(cells).toEqual(['Dr. Gómez', 'Dra. Ruiz'])
    await user.type(screen.getByLabelText('Buscar doctor'), 'gomez')
    expect(screen.queryByText('Dra. Ruiz')).not.toBeInTheDocument()
  })

  it('en móvil muestra tarjetas con nombre, contacto y acciones de 44 px', async () => {
    setMatchMedia(false)
    renderWithRouter(<DoctorsTable doctors={DOCTORS} onEdit={vi.fn()} onToggle={vi.fn()} />)
    expect(await screen.findByRole('list')).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'Editar Dra. Ruiz' })).toBeInTheDocument()
  })
})
```

Run: FAIL. Migrar `doctors-table.tsx` con el mismo patrón (columnas `name` title, `phone`/`email` subtitle con `font-mono` en teléfono, `active` badge, `actions`; `FEATURES = [filtering({ search: { id: 'doctores-buscar', label: 'Buscar doctor' } }), sorting(), pagination()]`; `emptyMessage` `(q) => q ? \`Ningún doctor coincide con "${q}"\` : 'Esta clínica aún no tiene doctores.'`). Run: PASS.

- [ ] **Step 4: Verificación en Chrome y commit**

Arrancar `pnpm dev`, entrar con el admin de `apps/api/.env`, `/configuracion/clinicas` y `/configuracion/clinicas/<id>` (pestaña Doctores) a 1280×800, 390×844 y 360×740: sin scroll horizontal de página, buscador con etiqueta, orden y paginación funcionando, consola limpia; capturas al reporte.

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test
git add apps/web/src/features/clinics apps/web/src/features/doctors
git commit -m "refactor(web): clínicas y doctores sobre DataGrid con búsqueda, orden y paginación

Refs #53"
```

---

### Task 8: Migrar usuarios y fases

**Files:**
- Modify: `apps/web/src/features/users/users-table.tsx`, `apps/web/src/features/stages/stages-table.tsx`
- Test: crear `apps/web/src/features/users/users-table.test.tsx`, `apps/web/src/features/stages/stages-table.test.tsx`

- [ ] **Step 1: Tests (RED)**

`users-table.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it, vi } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import type { User } from './api'
import { UsersTable } from './users-table'

const USERS = [
  { id: 'u1', name: 'Ana', email: 'ana@lab.local', role: 'recepcion', banned: false },
  { id: 'u2', name: 'Beto', email: 'beto@lab.local', role: 'tecnico', banned: true },
] as unknown as User[]

describe('UsersTable', () => {
  it('ordena por nombre, busca por correo y bloquea la acción sobre uno mismo', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<UsersTable users={USERS} currentUserId="u1" onEdit={vi.fn()} onToggleBanned={vi.fn()} />)
    await user.click(await screen.findByRole('button', { name: 'Ordenar por Nombre' }))
    await user.type(screen.getByLabelText('Buscar usuario'), 'beto@')
    expect(screen.queryByText('Ana')).not.toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'Desbloquear' })).toBeEnabled()
  })
})
```

`stages-table.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import { describe, expect, it, vi } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import type { Stage } from './api'
import { StagesTable } from './stages-table'

const STAGES = [
  { id: 's1', name: 'Recepción', color: '#111111', sort: 1, active: true },
  { id: 's2', name: 'Modelo', color: '#222222', sort: 2, active: true },
] as unknown as Stage[]

describe('StagesTable', () => {
  it('conserva el orden manual: sin botones de orden ni buscador, Subir deshabilitado en la primera', async () => {
    setMatchMedia(true)
    renderWithRouter(<StagesTable stages={STAGES} onEdit={vi.fn()} onToggle={vi.fn()} onMove={vi.fn()} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('button', { name: /Ordenar por/ })).not.toBeInTheDocument()
    expect(screen.queryByRole('search')).not.toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'Subir Recepción' })).toBeDisabled()
    expect(screen.getByRole('button', { name: 'Bajar Modelo' })).toBeDisabled()
  })
})
```

Run: FAIL.

- [ ] **Step 2: Migrar `users-table.tsx`**

`FEATURES = [filtering({ search: { id: 'usuarios-buscar', label: 'Buscar usuario', placeholder: 'Nombre o correo' } }), sorting(), pagination()]`. Columnas: `name` (title, `font-medium`), `email` (subtitle, `font-mono text-sm`), `role` (`cell: <Badge variant="outline">{ROLE_LABEL[...]}</Badge>`, `mobile: 'detail'`, `enableGlobalFilter: false`), `state` display (`banned ? <Badge variant="destructive">Bloqueado</Badge> : <ActiveBadge active />`, `mobile: 'badge'`), `actions` display con el mismo JSX de hoy (Editar + Bloquear/Desbloquear con `disabled={isSelf}` y `title`). `emptyMessage` `(q) => q ? \`Ningún usuario coincide con "${q}"\` : 'Aún no hay usuarios. Crea el primero con Nuevo usuario.'`.

- [ ] **Step 3: Migrar `stages-table.tsx`**

Sin features (`features: []`): columnas `#` (display, `cell: (c) => <span className="font-mono">{c.row.index + 1}</span>`, `meta: { width: 48, mobile: 'hidden' }`), `name` (title; en móvil el título muestra `{index + 1}. {name}` → definir `cell` que use `c.row.index`), `color` (swatch, `mobile: 'detail'`), `active` (badge), `actions` con Subir/Bajar/Editar/Switch usando `c.row.index` y `stages.length` para `disabled`. `emptyMessage: 'Aún no hay fases. Crea la primera con Nueva fase.'`. Solo `<DataGrid.Content />` dentro de `Root`.

Run ambos tests: PASS. Correr también `pnpm exec vitest run --project web` completo.

- [ ] **Step 4: Chrome y commit**

Verificar `/configuracion/usuarios` y `/configuracion/fases` en los tres viewports (switches 24×44, botones de 44 px en móvil, consola limpia).

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test
git add apps/web/src/features/users apps/web/src/features/stages
git commit -m "refactor(web): usuarios y fases sobre DataGrid (fases conserva el orden manual)

Refs #53"
```

---

### Task 9: Cierre del PR 1: E2E, docs iniciales y revisión

**Files:**
- Create: `docs/data-grid.md` (versión inicial)
- Modify: `apps/web/e2e/accesibilidad.spec.ts` (nada nuevo si las pantallas migradas pasan; si `TOUCH_CONTROLS` no cubre los botones de orden `icon-sm` de escritorio, no aplica en móvil porque no se renderizan ahí)

- [ ] **Step 1: E2E**

```bash
pnpm e2e:pr
```

Expected: todo en verde (login, configuración y accesibilidad de clínicas/fases no cambian de comportamiento). Si «configuración → clínicas: acciones de fila y switches» falla por un control nuevo menor de 44 px en móvil, corregir el control (no el test).

- [ ] **Step 2: `docs/data-grid.md` inicial**

Secciones: Qué es; Definir columnas (`defineColumns`, `meta.mobile`, `meta.filter`, `cellStyle`); Montar un grid (`useDataGrid` + `DataGrid.Root/Toolbar/Content/Pagination`); Features disponibles hasta ahora (`sorting`, `pagination`, `filtering`) con sus opciones; Tarjetas móviles (`meta.mobile` vs `renderCard`); Cómo añadir una feature (contrato `GridFeature`, regla de aislamiento, test con y sin la feature). Ejemplo completo: `clinics-table.tsx`.

- [ ] **Step 3: Verificación, commit y PR 1**

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test
git add docs/data-grid.md
git commit -m "docs: guía de uso del DataGrid (núcleo, orden, paginación y filtros)

Refs #53"
```

Revisión final de rama (`feat/data-grid-nucleo` contra `main`), ola de fixes y menú de integración → PR «DataGrid (1/3): núcleo sobre TanStack Table v9 y tablas de configuración» con `Refs #53`.

---

## Bloque B — agrupar, filtro avanzado, redimensionar; productos y precios (PR 2, rama `feat/data-grid-productos` desde la rama del PR 1)

### Task 10: Feature `resizing` con persistencia

**Files:**
- Create: `features/resizing.tsx`; Modify: `index.ts`, `parts/table.tsx` (ya lee `header.getSize()` cuando `has('resizing')`)
- Test: `features/resizing.test.tsx`

**Interfaces:**
- Produces: `resizing(): GridFeature` con slot `headerCell` (asa `role="separator"` con `aria-label="Redimensionar <col>"`, teclado ← → de 16 px, doble clic restablece), estado `columnSizing` leído de `readStored('datagrid:<key>:sizing', {})` y escrito al terminar (`columnResizeMode: 'onEnd'`).

- [ ] **Step 1: Test (RED)**

```tsx
import { fireEvent, screen } from '@testing-library/react'
import { afterEach, describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from '../data-grid'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { resizing } from './resizing'

type Row = { id: string; name: string }
const columns = defineColumns<Row>((col) => [col.accessor('name', { header: 'Nombre', meta: { width: 200 } })])
const rows: Row[] = [{ id: '1', name: 'Ana' }]

function Grid({ features }: { features: ReturnType<typeof resizing>[] }) {
  const grid = useDataGrid({ key: 'resize-test', columns, data: rows, features, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage="Vacío">
      <DataGrid.Content />
    </DataGrid.Root>
  )
}

describe('feature resizing', () => {
  afterEach(() => localStorage.clear())

  it('aplica el ancho inicial de meta.width y lo cambia con el teclado', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[resizing()]} />)
    const th = await screen.findByRole('columnheader', { name: /Nombre/ })
    expect(th).toHaveStyle({ width: '200px' })
    const handle = screen.getByRole('separator', { name: 'Redimensionar Nombre' })
    fireEvent.keyDown(handle, { key: 'ArrowRight' })
    expect(th).toHaveStyle({ width: '216px' })
    expect(localStorage.getItem('datagrid:resize-test:sizing:v1')).toContain('"name":216')
  })

  it('restaura el ancho guardado al montar', async () => {
    localStorage.setItem('datagrid:resize-test:sizing:v1', '{"name":300}')
    setMatchMedia(true)
    renderWithRouter(<Grid features={[resizing()]} />)
    expect(await screen.findByRole('columnheader', { name: /Nombre/ })).toHaveStyle({ width: '300px' })
  })

  it('sin la feature no hay asas', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[]} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('separator')).not.toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: Implementar `features/resizing.tsx`**

```tsx
import { columnResizingFeature, columnSizingFeature } from '@tanstack/react-table'
import { useGrid } from '../context'
import { readStored, writeStored } from '../storage'
import type { GridFeature, GridHeader } from '../types'

const STEP = 16

function ResizeHandle({ header }: { header: GridHeader<never> }) {
  const grid = useGrid()
  const column = header.column
  const label = column.columnDef.meta?.label ?? (typeof column.columnDef.header === 'string' ? column.columnDef.header : column.id)
  const persist = () => writeStored(`datagrid:${grid.key}:sizing`, grid.table.getState().columnSizing)
  return (
    <div
      role="separator"
      aria-label={`Redimensionar ${label}`}
      aria-orientation="vertical"
      tabIndex={0}
      onMouseDown={header.getResizeHandler()}
      onTouchStart={header.getResizeHandler()}
      onMouseUp={persist}
      onTouchEnd={persist}
      onDoubleClick={() => {
        column.resetSize()
        persist()
      }}
      onKeyDown={(e) => {
        if (e.key !== 'ArrowLeft' && e.key !== 'ArrowRight') return
        e.preventDefault()
        const next = Math.max(48, column.getSize() + (e.key === 'ArrowRight' ? STEP : -STEP))
        grid.table.setColumnSizing((prev) => ({ ...prev, [column.id]: next }))
        writeStored(`datagrid:${grid.key}:sizing`, { ...grid.table.getState().columnSizing, [column.id]: next })
      }}
      className={`ml-auto h-6 w-1 cursor-col-resize touch-none rounded bg-border hover:bg-primary focus-visible:outline-2 ${
        column.getIsResizing() ? 'bg-primary' : ''
      }`}
    />
  )
}

/** Anchos por columna con `meta.width` como tamaño inicial y persistencia local por `key`. */
export function resizing(): GridFeature {
  return {
    id: 'resizing',
    tanstack: { columnSizingFeature, columnResizingFeature },
    options: (init) => ({
      columnResizeMode: 'onEnd',
      enableColumnResizing: true,
      defaultColumn: { minSize: 48 },
      initialState: undefined, // el initialState va abajo
      ...({} as Record<string, unknown>),
      // El estado inicial depende de `key`, así que se calcula aquí y no en `initialState` estático.
      ...(() => ({ initialState: { columnSizing: readStored<Record<string, number>>(`datagrid:${init.key}:sizing`, {}) } }))(),
    }),
    slots: { headerCell: ResizeHandle },
  }
}
```

Simplificar: `useDataGrid` debe fusionar `options().initialState` con el `initialState` de las features (hoy solo fusiona `feature.initialState`). Ajustar `useDataGrid` para que, si `options()` devuelve `initialState`, se fusione con el acumulado, y quitar las dos líneas de relleno (`initialState: undefined` y el spread vacío). `meta.width` → `size` de columna: en `defineColumns` no se traduce; en `useDataGrid` mapear `columns.map(c => c.meta?.width ? { ...c, size: c.meta.width } : c)`. Añadir test en `use-data-grid.test.tsx`: «meta.width se aplica como size».

Run: PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): redimensionado de columnas del DataGrid con teclado y persistencia local

Refs #53"
```

---

### Task 11: Feature `grouping` con filas de grupo y agregados

**Files:**
- Create: `features/grouping.tsx`; Modify: `parts/table.tsx` (filas de grupo), `parts/cards.tsx` (encabezados de grupo), `index.ts`
- Test: `features/grouping.test.tsx`

**Interfaces:**
- Produces: `grouping(opts?: { initial?: string }): GridFeature` con slot `toolbar` (select «Agrupar por» con las columnas `meta.groupable`) y `columnMenu` (item «Agrupar por esta columna» / «Quitar agrupación»). Filas de grupo: botón expandir/contraer con `aria-expanded`, texto `<valor> (<n>)`, celdas agregadas con `meta.aggregate` (`sum` → suma formateada con `toFixed(2)`; `count` → n).

- [ ] **Step 1: Test (RED)**

```tsx
import { screen, within } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from '../data-grid'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { grouping } from './grouping'

type Row = { id: string; name: string; category: string; price: number }
const columns = defineColumns<Row>((col) => [
  col.accessor('name', { header: 'Producto' }),
  col.accessor('category', { header: 'Categoría', meta: { groupable: true } }),
  col.accessor('price', { header: 'Precio', meta: { aggregate: 'sum', align: 'right' } }),
])
const rows: Row[] = [
  { id: '1', name: 'Zirconio', category: 'Fija', price: 45 },
  { id: '2', name: 'Metal porcelana', category: 'Fija', price: 30 },
  { id: '3', name: 'Acrílico', category: 'Removible', price: 20 },
]

function Grid({ features }: { features: ReturnType<typeof grouping>[] }) {
  const grid = useDataGrid({ key: 'test', columns, data: rows, features, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage="Vacío">
      <DataGrid.Toolbar />
      <DataGrid.Content />
    </DataGrid.Root>
  )
}

describe('feature grouping', () => {
  it('agrupa por categoría con conteo, agregado y filas expandibles', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={[grouping()]} />)
    await user.selectOptions(await screen.findByLabelText('Agrupar por'), 'category')
    const fija = screen.getByRole('row', { name: /Fija \(2\)/ })
    expect(within(fija).getByText('75.00')).toBeInTheDocument()
    const toggle = within(fija).getByRole('button', { name: /Contraer Fija/ })
    expect(toggle).toHaveAttribute('aria-expanded', 'true')
    await user.click(toggle)
    expect(screen.queryByText('Zirconio')).not.toBeInTheDocument()
  })

  it('arranca agrupado si se indica initial', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[grouping({ initial: 'category' })]} />)
    expect(await screen.findByRole('row', { name: /Removible \(1\)/ })).toBeInTheDocument()
  })

  it('sin la feature no hay «Agrupar por»', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[]} />)
    await screen.findByRole('table')
    expect(screen.queryByLabelText('Agrupar por')).not.toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: Implementar `features/grouping.tsx`**

```tsx
import {
  aggregationFns,
  columnGroupingFeature,
  createExpandedRowModel,
  createGroupedRowModel,
  rowAggregationFeature,
  rowExpandingFeature,
} from '@tanstack/react-table'
import { useGrid } from '../context'
import { DropdownMenuItem } from '@/components/ui/dropdown-menu'
import { Label } from '@/components/ui/label'
import type { GridColumn, GridFeature } from '../types'

const labelOf = (column: GridColumn<never>) =>
  column.columnDef.meta?.label ?? (typeof column.columnDef.header === 'string' ? column.columnDef.header : column.id)

function GroupBySelect() {
  const grid = useGrid()
  const { table } = grid
  const groupable = table.getAllLeafColumns().filter((c) => c.columnDef.meta?.groupable)
  const current = table.getState().grouping[0] ?? ''
  return (
    <div className="flex flex-col gap-1.5">
      <Label htmlFor="agrupar-por">Agrupar por</Label>
      <select
        id="agrupar-por"
        className="h-11 rounded-md border border-input bg-background px-3 text-sm"
        value={current}
        onChange={(e) => table.setGrouping(e.target.value ? [e.target.value] : [])}
      >
        <option value="">Sin agrupar</option>
        {groupable.map((c) => (
          <option key={c.id} value={c.id}>
            {labelOf(c)}
          </option>
        ))}
      </select>
    </div>
  )
}

function GroupMenuItem({ column }: { column: GridColumn<never> }) {
  if (!column.columnDef.meta?.groupable) return null
  const grouped = column.getIsGrouped()
  return (
    <DropdownMenuItem onSelect={() => column.toggleGrouping()}>
      {grouped ? 'Quitar agrupación' : `Agrupar por ${labelOf(column)}`}
    </DropdownMenuItem>
  )
}

/** Agrupación por una columna `groupable`, filas de grupo expandibles y agregados por `meta.aggregate`. */
export function grouping(opts: { initial?: string } = {}): GridFeature {
  return {
    id: 'grouping',
    tanstack: {
      columnGroupingFeature,
      rowExpandingFeature,
      rowAggregationFeature,
      groupedRowModel: createGroupedRowModel(),
      expandedRowModel: createExpandedRowModel(),
      aggregationFns,
    },
    initialState: { grouping: opts.initial ? [opts.initial] : [], expanded: true },
    slots: { toolbar: GroupBySelect, columnMenu: GroupMenuItem },
  }
}
```

`meta.aggregate` → `aggregationFn` de columna: en `useDataGrid`, junto al mapeo de `meta.width`, mapear `meta.aggregate` a `aggregationFn: meta.aggregate` y `aggregatedCell: (c) => formatAggregate(c.getValue())` donde `formatAggregate` da `toFixed(2)` para números de `sum` y el entero para `count` (definir en `use-data-grid.ts`).

`parts/table.tsx`, en el cuerpo: si `grid.has('grouping') && row.getIsGrouped()`, renderizar una fila de grupo:

```tsx
<TableRow key={row.id} className="bg-muted/40 font-medium" aria-label={`${String(row.groupingValue)} (${row.subRows.length})`}>
  {row.getAllCells().map((cell) => (
    <TableCell key={cell.id} className={cell.column.columnDef.meta?.align === 'right' ? 'text-right' : ''}>
      {cell.getIsGrouped() ? (
        <button type="button" className="inline-flex h-11 items-center gap-2" aria-expanded={row.getIsExpanded()}
          aria-label={`${row.getIsExpanded() ? 'Contraer' : 'Expandir'} ${String(row.groupingValue)}`}
          onClick={row.getToggleExpandedHandler()}>
          <ChevronRight aria-hidden className={`size-4 transition-transform ${row.getIsExpanded() ? 'rotate-90' : ''}`} />
          {String(row.groupingValue)} ({row.subRows.length})
        </button>
      ) : cell.getIsAggregated() ? (
        <table.FlexRender cell={cell} />
      ) : null}
    </TableCell>
  ))}
</TableRow>
```

Nota: `role="row"` con `aria-label` es lo que permite `getByRole('row', { name })` en el test. En `parts/cards.tsx`: si la fila está agrupada, renderizar `<li>` de encabezado (`<h3>` con valor y conteo) seguido de sus `subRows` como tarjetas (recorrer `table.getRowModel().rows` y aplanar `row.getIsGrouped() ? row.subRows : [row]`, con encabezado antes de cada grupo).

Run: PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): agrupación del DataGrid con filas de grupo expandibles y agregados

Refs #53"
```

---

### Task 12: Feature `advancedFilter`

**Files:**
- Create: `features/advanced-filter.tsx`, `features/advanced-filter-dialog.tsx`, `features/advanced-filter-logic.ts`; Modify: `index.ts`
- Test: `features/advanced-filter-logic.test.ts`, `features/advanced-filter.test.tsx`

**Interfaces:**
- Produces: `advancedFilter(): GridFeature` (solo `mode: 'client'`) con slot `toolbar` (botón «Filtro avanzado» + chips de condiciones activas). Tipos: `Condition = { column: string; op: Operator; value: string }`, `Operator = 'contiene' | 'es' | 'no_es' | 'empieza' | 'mayor' | 'menor' | 'entre'`, `AdvancedFilter = { logic: 'and' | 'or'; conditions: Condition[] }`. Función pura `matches(row: Record<string, unknown>, filter: AdvancedFilter): boolean` en `advanced-filter-logic.ts`.

- [ ] **Step 1: Test de la lógica pura (RED)**

`advanced-filter-logic.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { matches, type AdvancedFilter } from './advanced-filter-logic'

const row = { name: 'Prótesis híbrida', category: 'Removible', days: 12 }
const f = (logic: 'and' | 'or', ...conditions: AdvancedFilter['conditions']): AdvancedFilter => ({ logic, conditions })

describe('matches (filtro avanzado)', () => {
  it('contiene / es / no es / empieza, sin acentos', () => {
    expect(matches(row, f('and', { column: 'name', op: 'contiene', value: 'hibrida' }))).toBe(true)
    expect(matches(row, f('and', { column: 'category', op: 'es', value: 'removible' }))).toBe(true)
    expect(matches(row, f('and', { column: 'category', op: 'no_es', value: 'Removible' }))).toBe(false)
    expect(matches(row, f('and', { column: 'name', op: 'empieza', value: 'pro' }))).toBe(true)
  })
  it('mayor / menor / entre sobre números', () => {
    expect(matches(row, f('and', { column: 'days', op: 'mayor', value: '10' }))).toBe(true)
    expect(matches(row, f('and', { column: 'days', op: 'menor', value: '10' }))).toBe(false)
    expect(matches(row, f('and', { column: 'days', op: 'entre', value: '10,15' }))).toBe(true)
  })
  it('AND exige todas; OR basta una; sin condiciones pasa todo', () => {
    expect(matches(row, f('and', { column: 'days', op: 'mayor', value: '10' }, { column: 'category', op: 'es', value: 'Fija' }))).toBe(false)
    expect(matches(row, f('or', { column: 'days', op: 'mayor', value: '10' }, { column: 'category', op: 'es', value: 'Fija' }))).toBe(true)
    expect(matches(row, f('and'))).toBe(true)
  })
})
```

Run: FAIL.

- [ ] **Step 2: Implementar `advanced-filter-logic.ts`**

```ts
import { normalize } from '../lib/normalize'

export type Operator = 'contiene' | 'es' | 'no_es' | 'empieza' | 'mayor' | 'menor' | 'entre'
export type Condition = { column: string; op: Operator; value: string }
export type AdvancedFilter = { logic: 'and' | 'or'; conditions: Condition[] }

export const OPERATOR_LABEL: Record<Operator, string> = {
  contiene: 'contiene', es: 'es', no_es: 'no es', empieza: 'empieza con', mayor: 'mayor que', menor: 'menor que', entre: 'entre',
}
export const TEXT_OPERATORS: Operator[] = ['contiene', 'es', 'no_es', 'empieza']
export const NUMBER_OPERATORS: Operator[] = ['es', 'no_es', 'mayor', 'menor', 'entre']

function matchesOne(cell: unknown, c: Condition): boolean {
  const text = normalize(cell)
  const wanted = normalize(c.value)
  switch (c.op) {
    case 'contiene': return text.includes(wanted)
    case 'es': return text === wanted
    case 'no_es': return text !== wanted
    case 'empieza': return text.startsWith(wanted)
    case 'mayor': return Number(cell) > Number(c.value)
    case 'menor': return Number(cell) < Number(c.value)
    case 'entre': {
      const [a, b] = c.value.split(',').map(Number)
      const n = Number(cell)
      return a !== undefined && b !== undefined && n >= a && n <= b
    }
  }
}

/** Evalúa todas las condiciones con AND u OR; sin condiciones, la fila pasa. */
export function matches(row: Record<string, unknown>, filter: AdvancedFilter): boolean {
  if (filter.conditions.length === 0) return true
  const results = filter.conditions.map((c) => matchesOne(row[c.column], c))
  return filter.logic === 'and' ? results.every(Boolean) : results.some(Boolean)
}
```

Run: PASS.

- [ ] **Step 3: Test del componente (RED)**

`advanced-filter.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from '../data-grid'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { advancedFilter } from './advanced-filter'

type Row = { id: string; name: string; days: number }
const columns = defineColumns<Row>((col) => [
  col.accessor('name', { header: 'Producto', meta: { filter: 'text' } }),
  col.accessor('days', { header: 'Días', meta: { filter: 'range' } }),
])
const rows: Row[] = [
  { id: '1', name: 'Zirconio', days: 5 },
  { id: '2', name: 'Acrílico', days: 12 },
]

function Grid({ features }: { features: ReturnType<typeof advancedFilter>[] }) {
  const grid = useDataGrid({ key: 'test', columns, data: rows, features, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage="Vacío">
      <DataGrid.Toolbar />
      <DataGrid.Content />
    </DataGrid.Root>
  )
}

describe('feature advancedFilter', () => {
  it('aplica una condición desde el diálogo y la muestra como chip', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={[advancedFilter()]} />)
    await user.click(await screen.findByRole('button', { name: 'Filtro avanzado' }))
    const dialog = await screen.findByRole('dialog', { name: 'Filtro avanzado' })
    await user.click(screen.getByRole('button', { name: 'Añadir condición' }))
    await user.selectOptions(screen.getByLabelText('Columna 1'), 'days')
    await user.selectOptions(screen.getByLabelText('Operador 1'), 'mayor')
    await user.type(screen.getByLabelText('Valor 1'), '10')
    await user.click(screen.getByRole('button', { name: 'Aplicar' }))
    expect(dialog).not.toBeInTheDocument()
    expect(screen.queryByText('Zirconio')).not.toBeInTheDocument()
    expect(screen.getByText('Acrílico')).toBeInTheDocument()
    expect(screen.getByText('Días mayor que 10')).toBeInTheDocument()
    await user.click(screen.getByRole('button', { name: 'Quitar condición Días mayor que 10' }))
    expect(screen.getByText('Zirconio')).toBeInTheDocument()
  })

  it('sin la feature no hay botón', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[]} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('button', { name: 'Filtro avanzado' })).not.toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 4: Implementar `advanced-filter.tsx` y `advanced-filter-dialog.tsx`**

La feature reutiliza `columnFilteringFeature` con un filtro **global** propio: registra `tanstack: { columnFilteringFeature, globalFilteringFeature, filteredRowModel: createFilteredRowModel(), filterFns: { advanced: advancedFilterFn } }` y guarda el `AdvancedFilter` en un estado propio de React dentro del slot; para aplicarlo sin chocar con la búsqueda global de `filtering`, usa `table.options.meta`? No: más simple y aislado, la feature filtra **los datos antes de la tabla**: `options: () => ({ })` no basta. Solución elegida: `useDataGrid` acepta que una feature aporte `transformData?: (rows: T[]) => T[]` (nuevo campo opcional en `GridFeature`, aplicado con `useMemo` antes de `useTable`). `advancedFilter` guarda su `AdvancedFilter` en un `useSyncExternalStore` mínimo por `key` (módulo `advanced-filter-store.ts`: `Map<key, { filter, listeners }>`), y `transformData` filtra con `matches(row as Record<string, unknown>, filter)`. Añadir el campo a `types.ts` y el `useMemo` en `use-data-grid.ts` con test («transformData de una feature se aplica a los datos»).

`advanced-filter.tsx`: botón «Filtro avanzado» (`Button variant="outline" className="h-11"`), chips `<span>` con «`<Columna> <operador> <valor>`» y botón «Quitar condición …» (`icon` 44 px), y `AdvancedFilterDialog`.

`advanced-filter-dialog.tsx`: `Dialog` de shadcn con `DialogTitle` «Filtro avanzado»; select «Combinar con» (Todas las condiciones (Y) / Cualquiera (O)) `id="logica"`; lista de filas con `Label htmlFor` «Columna N», «Operador N», «Valor N» (selects nativos + `Input`), botón «Añadir condición», botón «Quitar» por fila, pie con «Cancelar» y «Aplicar». Las columnas ofrecidas: las que tienen `meta.filter`; operadores según `meta.filter === 'range' ? NUMBER_OPERATORS : TEXT_OPERATORS`; para `entre`, el valor se escribe como `10,15` con placeholder «mín,máx».

Run: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): filtro avanzado del DataGrid (columna · operador · valor con Y/O)

Refs #53"
```

---

### Task 13: Migrar productos (grouping, filtros, filtro avanzado, resizing) + E2E

**Files:**
- Modify: `apps/web/src/features/products/products-table.tsx`, `apps/web/src/features/products/products-table.test.tsx`
- Modify: `apps/web/e2e/configuracion.spec.ts` (nuevo test `@clave`)

- [ ] **Step 1: Tests nuevos (RED)** en `products-table.test.tsx`:

```tsx
  it('agrupa por categoría con conteo por grupo', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<ProductsTable products={PRODUCTS} onEdit={vi.fn()} onToggle={vi.fn()} />)
    await user.selectOptions(await screen.findByLabelText('Agrupar por'), 'category')
    expect(screen.getByRole('row', { name: /Prótesis fija \(1\)/ })).toBeInTheDocument()
  })

  it('filtra por categoría y por búsqueda sin acentos', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<ProductsTable products={PRODUCTS} onEdit={vi.fn()} onToggle={vi.fn()} />)
    await user.selectOptions(await screen.findByLabelText('Filtrar Categoría'), 'Prótesis fija')
    expect(screen.queryByText('Prótesis híbrida')).not.toBeInTheDocument()
  })
```

(Importar `userEvent`.) Run: FAIL.

- [ ] **Step 2: Migrar `products-table.tsx`**

`FEATURES = [filtering({ search: { id: 'productos-buscar', label: 'Buscar producto', placeholder: 'Código o nombre' }, columns: true }), advancedFilter(), grouping(), sorting(), resizing(), pagination()]`.

Columnas: `code` (`font-mono text-sm`, `mobile: 'subtitle'`, `meta.width: 96`), `name` (title, `truncate` con `title`, `width: 220`, `filter: 'text'`), `category` accessor `(p) => p.category?.name ?? '—'` con `id: 'category'`, `header: 'Categoría'`, `meta: { filter: 'select', groupable: true, mobile: 'subtitle', width: 160 }`, `pricingUnit` (`cell: PRICING_UNIT_LABEL[...]`, header «Se cobra», `mobile: 'detail'`, `enableGlobalFilter: false`), `basePrice` accessor `(p) => Number(p.basePrice)` id `price` header «Precio base» `cell: formatMoney(String(...))` `meta: { align: 'right', aggregate: 'sum', mobile: 'detail' }` (mostrar `$` con `formatMoney` en `aggregatedCell` también: pasar `aggregatedCell` en la columna), `turnaroundDays` id `days` header «Días» con el icono «Requiere prueba» igual que hoy (`filter: 'range'`), `active` (badge, `enableGlobalFilter: false`), `actions`. Mantener `emptyMessage` con búsqueda.

Run tests de productos (3 existentes + 2 nuevos): PASS.

- [ ] **Step 3: E2E `@clave`** en `configuracion.spec.ts`, dentro de `describe('Configuración')`:

```ts
  test('agrupa productos por categoría y filtra con el filtro avanzado', { tag: '@clave' }, async ({ page }) => {
    await page.goto('/configuracion/productos')
    await page.getByLabel('Agrupar por').selectOption('category')
    await expect(page.getByRole('row', { name: /Prótesis fija \(\d+\)/ })).toBeVisible()
    await page.getByRole('button', { name: 'Filtro avanzado' }).click()
    await page.getByRole('button', { name: 'Añadir condición' }).click()
    await page.getByLabel('Columna 1').selectOption('name')
    await page.getByLabel('Operador 1').selectOption('contiene')
    await page.getByLabel('Valor 1').fill('zirconio')
    await page.getByRole('button', { name: 'Aplicar' }).click()
    await expect(page.getByText('Producto contiene zirconio')).toBeVisible()
    await expect(page.getByRole('row', { name: /Prótesis removible/ })).toHaveCount(0)
  })
```

Run: `pnpm e2e:pr` → verde (los seeds tienen ambas categorías y «Zirconio»).

- [ ] **Step 4: Chrome (1280/390/360: agrupar, redimensionar con teclado, filtro avanzado en móvil) y commit**

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test
git add apps/web/src/features/products/products-table.tsx apps/web/src/features/products/products-table.test.tsx apps/web/e2e/configuracion.spec.ts
git commit -m "refactor(web): productos sobre DataGrid con agrupación, filtros, filtro avanzado y redimensionado

Refs #53"
```

---

### Task 14: Migrar precios especiales (borradores intactos)

**Files:**
- Modify: `apps/web/src/features/products/clinic-prices-table.tsx`
- Test: `clinic-prices-table.test.tsx` (los 5 existentes deben seguir en verde sin cambios; añadir 1)

- [ ] **Step 1: Test nuevo (RED)**

```tsx
  it('ordena por producto', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithProviders(<ClinicPricesTable clinicId="clinic-1" />)
    await screen.findByText(/Zirconio/)
    await user.click(screen.getByRole('button', { name: 'Ordenar por Producto' }))
    const first = screen.getAllByRole('row')[1]
    expect(first).toHaveTextContent('Prótesis híbrida')
  })
```

Run: FAIL.

- [ ] **Step 2: Migrar**

Conservar `PriceCell`, `drafts`, `seenClinicId` y `normalize` local desaparece (usar el buscador de `filtering`). `FEATURES = [filtering({ search: { id: 'precios-buscar', label: 'Buscar producto', placeholder: 'Buscar por código o nombre' } }), sorting(), pagination()]`. Columnas: `product` accessor `(p) => \`${p.code} ${p.name}\`` id `product` header «Producto» con `cell` que pinta `<span className="font-mono text-xs">{code}</span> · {name}` (`mobile: 'title'`), `basePrice` id `base` header «Precio base» (`font-mono`, `align: 'right'`, `mobile: 'subtitle'`, `enableGlobalFilter: false`), `special` display header «Precio para esta clínica» con `cell: (c) => priceCell(c.row.original)` (`mobile: 'actions'`, `enableSorting: false`). `emptyMessage` `(q) => q ? \`Ningún producto coincide con "${q}"\` : 'No hay productos activos.'`. El «Cargando…» sigue antes del grid.

Run los 6 tests: PASS.

- [ ] **Step 3: Chrome (pestaña Precios especiales de una clínica) y commit**

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm e2e:pr
git add apps/web/src/features/products/clinic-prices-table.tsx apps/web/src/features/products/clinic-prices-table.test.tsx
git commit -m "refactor(web): precios especiales sobre DataGrid conservando los borradores por producto

Refs #53"
```

Revisión final de rama, ola de fixes y PR «DataGrid (2/3): agrupación, filtro avanzado y redimensionado; productos y precios» (`Refs #53`, base = rama del PR 1 si aún no se mergeó).

---

## Bloque C — `orden` en la API, fijar columnas, URL y trabajos; cierre (PR 3, rama `feat/data-grid-trabajos`)

### Task 15: Parámetro `orden` en la API de trabajos

**Files:**
- Modify: `packages/shared/src/schemas/cases.ts:122-133`, `packages/shared/src/schemas/cases.test.ts`
- Modify: `apps/api/src/features/cases/repo.ts` (función `list…`, líneas ~170-176 `orderBy`), `apps/api/src/features/cases/cases.test.ts`

**Interfaces:**
- Produces: `CaseListQuery.orden?: 'codigo' | 'codigo-desc' | 'entrega' | 'entrega-desc' | 'clinica' | 'clinica-desc' | 'estado' | 'estado-desc'`; exportar `CASE_ORDERS` (`as const`) desde shared.

- [ ] **Step 1: Test shared (RED)** en `cases.test.ts` de shared:

```ts
  it('caseListQuerySchema acepta orden válido y rechaza otro', () => {
    expect(caseListQuerySchema.parse({ orden: 'entrega-desc' }).orden).toBe('entrega-desc')
    expect(caseListQuerySchema.safeParse({ orden: 'precio' }).success).toBe(false)
    expect(caseListQuerySchema.parse({}).orden).toBeUndefined()
  })
```

Run: `pnpm exec vitest run --project shared src/schemas/cases.test.ts` → FAIL.

- [ ] **Step 2: Schema**

```ts
export const CASE_ORDERS = [
  'codigo', 'codigo-desc', 'entrega', 'entrega-desc', 'clinica', 'clinica-desc', 'estado', 'estado-desc',
] as const
export type CaseOrder = (typeof CASE_ORDERS)[number]
// dentro de caseListQuerySchema, antes de pagina:
  orden: z.enum(CASE_ORDERS, { error: 'Orden inválido' }).optional(),
```

Exportar `CASE_ORDERS`/`CaseOrder` desde `packages/shared/src/index.ts` si los schemas no se reexportan con `export *`. Run: PASS.

- [ ] **Step 3: Test API (RED)** en `apps/api/src/features/cases/cases.test.ts`, tras «listado por vista y búsqueda»:

```ts
  it('ordena por entrega, código, clínica y estado en ambas direcciones; sin orden, urgentes primero', async () => {
    const tarde = await createOne(recepcion, { patientRef: 'Tarde', dueDate: '2030-01-10' })
    const pronto = await createOne(recepcion, { patientRef: 'Pronto', dueDate: '2030-01-01' })
    const ids = async (qs: string) =>
      ((await (await app.request(`/api/trabajos${qs}`, req(recepcion, 'GET'))).json()) as { cases: { id: string }[] }).cases.map((c) => c.id)
    expect(await ids('?orden=entrega')).toEqual([pronto, tarde])
    expect(await ids('?orden=entrega-desc')).toEqual([tarde, pronto])
    expect(await ids('?orden=codigo')).toEqual([tarde, pronto]) // el primero creado tiene el código menor
    expect(await ids('?orden=codigo-desc')).toEqual([pronto, tarde])
    expect((await app.request('/api/trabajos?orden=precio', req(recepcion, 'GET'))).status).toBe(422)
  })
```

Adaptar `createOne` si no acepta `dueDate` (mirar su firma en el archivo; si no, hacer `ctx.db.update(cases).set({ dueDate })` como en el test de vista). Run: FAIL (orden ignorado → el orden por defecto no coincide en al menos una aserción).

- [ ] **Step 4: Repo**

En `repo.ts`, sustituir el `.orderBy(...)` de la lista:

```ts
const ORDER_COLUMNS = {
  codigo: () => cases.code,
  entrega: () => effectiveDate,
  clinica: () => clinics.name,
  estado: () => cases.status,
} as const

function orderFor(orden: CaseListQuery['orden']) {
  const urgentFirst = desc(sql`${cases.priority} = 'urgente'`)
  if (!orden) return [urgentFirst, sql`${effectiveDate} asc nulls last`, desc(cases.code)]
  const [field, dir] = orden.split('-') as [keyof typeof ORDER_COLUMNS, 'desc' | undefined]
  const col = ORDER_COLUMNS[field]()
  return [urgentFirst, dir === 'desc' ? sql`${col} desc nulls last` : sql`${col} asc nulls last`, desc(cases.code)]
}
// …
.orderBy(...orderFor(q.orden))
```

(`effectiveDate` ya existe en el archivo como expresión SQL; `clinics` ya está unido.) Run test de API: PASS. El servicio no cambia (pasa `q` entero al repo); añadir en `service.test.ts` una aserción de que `orden` llega al fake: en `fakes.ts` el `list` recibe `q`; test «la lista pasa `orden` al repositorio» que registra el último `q` recibido.

- [ ] **Step 5: Verificación y commit**

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test
git add packages/shared/src apps/api/src/features/cases
git commit -m "feat(api): orden por columna en la lista de trabajos (codigo, entrega, clinica, estado)

Refs #53"
```

---

### Task 16: Feature `pinning` con columnas sticky

**Files:**
- Create: `features/pinning.tsx`; Modify: `parts/pinning-styles.ts`, `index.ts`
- Test: `features/pinning.test.tsx`

**Interfaces:**
- Produces: `pinning(opts?: { left?: string[]; right?: string[] }): GridFeature` con `columnMenu` (items «Fijar a la izquierda», «Fijar a la derecha», «Soltar») y persistencia `datagrid:<key>:pinning`. `pinningStyles(column, true)` devuelve `position: sticky` + `insetInlineStart/End` + sombra en la última fijada.

- [ ] **Step 1: Test (RED)**

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { afterEach, describe, expect, it } from 'vitest'
import { setMatchMedia } from '@/test/match-media'
import { renderWithRouter } from '@/test/router'
import { DataGrid } from '../data-grid'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { pinning } from './pinning'

type Row = { id: string; code: string; name: string }
const columns = defineColumns<Row>((col) => [
  col.accessor('code', { header: 'Código' }),
  col.accessor('name', { header: 'Nombre' }),
])
const rows: Row[] = [{ id: '1', code: '26-00001', name: 'Ana' }]

function Grid({ features }: { features: ReturnType<typeof pinning>[] }) {
  const grid = useDataGrid({ key: 'pin-test', columns, data: rows, features, getRowId: (r) => r.id })
  return (
    <DataGrid.Root grid={grid} emptyMessage="Vacío">
      <DataGrid.Content />
    </DataGrid.Root>
  )
}

describe('feature pinning', () => {
  afterEach(() => localStorage.clear())

  it('fija la columna inicial con position sticky y permite soltarla desde el menú', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    renderWithRouter(<Grid features={[pinning({ left: ['code'] })]} />)
    const th = await screen.findByRole('columnheader', { name: /Código/ })
    expect(th).toHaveStyle({ position: 'sticky' })
    await user.click(screen.getByRole('button', { name: 'Opciones de la columna Código' }))
    await user.click(await screen.findByRole('menuitem', { name: 'Soltar' }))
    expect(th).not.toHaveStyle({ position: 'sticky' })
    expect(localStorage.getItem('datagrid:pin-test:pinning:v1')).toContain('"start":[]')
  })

  it('sin la feature no hay menú de columna ni sticky', async () => {
    setMatchMedia(true)
    renderWithRouter(<Grid features={[]} />)
    await screen.findByRole('table')
    expect(screen.queryByRole('button', { name: /Opciones de la columna/ })).not.toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: Implementar**

`features/pinning.tsx`:

```tsx
import { columnPinningFeature } from '@tanstack/react-table'
import { useGrid } from '../context'
import { DropdownMenuItem } from '@/components/ui/dropdown-menu'
import { readStored, writeStored } from '../storage'
import type { GridColumn, GridFeature } from '../types'

type PinState = { start: string[]; end: string[] }

function PinMenuItems({ column }: { column: GridColumn<never> }) {
  const grid = useGrid()
  const persist = () => writeStored(`datagrid:${grid.key}:pinning`, grid.table.getState().columnPinning)
  const pin = (pos: 'start' | 'end' | false) => {
    column.pin(pos)
    persist()
  }
  const pinned = column.getIsPinned()
  return (
    <>
      {pinned !== 'start' && <DropdownMenuItem onSelect={() => pin('start')}>Fijar a la izquierda</DropdownMenuItem>}
      {pinned !== 'end' && <DropdownMenuItem onSelect={() => pin('end')}>Fijar a la derecha</DropdownMenuItem>}
      {pinned && <DropdownMenuItem onSelect={() => pin(false)}>Soltar</DropdownMenuItem>}
    </>
  )
}

/** Columnas fijadas a izquierda (`start`) o derecha (`end`), con persistencia local por `key`. */
export function pinning(opts: { left?: string[]; right?: string[] } = {}): GridFeature {
  return {
    id: 'pinning',
    tanstack: { columnPinningFeature },
    options: (init) => ({
      enableColumnPinning: true,
      initialState: {
        columnPinning: readStored<PinState>(`datagrid:${init.key}:pinning`, { start: opts.left ?? [], end: opts.right ?? [] }),
      },
    }),
    slots: { columnMenu: PinMenuItems },
  }
}
```

`parts/pinning-styles.ts`:

```ts
import type { CSSProperties } from 'react'
import type { GridColumn } from '../types'

/** Estilos sticky de una columna fijada (patrón del ejemplo oficial column-pinning-sticky de TanStack). */
export function pinningStyles(column: GridColumn<never>, hasPinning: boolean): CSSProperties {
  if (!hasPinning) return {}
  const pinned = column.getIsPinned()
  if (!pinned) return {}
  const lastLeft = pinned === 'start' && column.getIsLastColumn('start')
  const firstRight = pinned === 'end' && column.getIsFirstColumn('end')
  return {
    position: 'sticky',
    insetInlineStart: pinned === 'start' ? `${column.getStart('start')}px` : undefined,
    insetInlineEnd: pinned === 'end' ? `${column.getAfter('end')}px` : undefined,
    zIndex: 1,
    background: 'var(--card)',
    boxShadow: lastLeft ? 'inset -1px 0 0 var(--border)' : firstRight ? 'inset 1px 0 0 var(--border)' : undefined,
  }
}
```

(Confirmar el nombre del token de fondo de tarjeta en `apps/web/src/index.css`; usar el mismo que `bg-card`.) `column.getStart('start')` necesita anchos: sin `resizing`, TanStack usa `size` por defecto (150) — aceptable; con `resizing` usa los reales.

Run: PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): columnas fijadas del DataGrid con sticky y persistencia local

Refs #53"
```

---

### Task 17: Feature `urlState`

**Files:**
- Create: `features/url-state.ts`; Modify: `index.ts`, `use-data-grid.ts` (fusionar `options().state`/`on*Change`; ya lo hace por spread)
- Test: `features/url-state.test.tsx`

**Interfaces:**
- Produces: `urlState(opts: { search: UrlSearch; navigate: (patch: Record<string, unknown>) => void; pageSize: number }): GridFeature` donde `UrlSearch = { pagina?: number; orden?: string; q?: string }`. Mapeos: `pagina` ↔ `pagination.pageIndex + 1`; `orden` `'campo' | 'campo-desc'` ↔ `sorting: [{ id: campo, desc }]`; `q` ↔ `globalFilter`. Al cambiar orden o `q`, `navigate({ …, pagina: undefined })`. Helpers puros exportados: `ordenToSorting(orden?)`, `sortingToOrden(sorting)`.

- [ ] **Step 1: Test (RED)**

```tsx
import { renderHook } from '@testing-library/react'
import { describe, expect, it, vi } from 'vitest'
import { defineColumns } from '../define-columns'
import { useDataGrid } from '../use-data-grid'
import { ordenToSorting, sortingToOrden, urlState } from './url-state'
import { pagination } from './pagination'
import { sorting } from './sorting'

describe('urlState: mapeo puro', () => {
  it('orden ↔ sorting', () => {
    expect(ordenToSorting('entrega-desc')).toEqual([{ id: 'entrega', desc: true }])
    expect(ordenToSorting('codigo')).toEqual([{ id: 'codigo', desc: false }])
    expect(ordenToSorting(undefined)).toEqual([])
    expect(sortingToOrden([{ id: 'clinica', desc: true }])).toBe('clinica-desc')
    expect(sortingToOrden([])).toBeUndefined()
  })
})

type Row = { id: string; codigo: string }
const columns = defineColumns<Row>((col) => [col.accessor('codigo', { header: 'Código' })])

describe('urlState: enlaza el estado del grid con la URL', () => {
  it('lee pagina/orden/q de search y navega al cambiar, reseteando pagina', () => {
    const navigate = vi.fn()
    const features = [
      sorting(),
      pagination({ pageSize: 25 }),
      urlState({ search: { pagina: 3, orden: 'codigo-desc', q: 'ana' }, navigate, pageSize: 25 }),
    ]
    const { result } = renderHook(() =>
      useDataGrid({ key: 't', columns, data: [], features, mode: 'server', rowCount: 100, getRowId: (r) => r.id }),
    )
    const state = result.current.table.getState()
    expect(state.pagination).toEqual({ pageIndex: 2, pageSize: 25 })
    expect(state.sorting).toEqual([{ id: 'codigo', desc: true }])
    expect(state.globalFilter).toBe('ana')

    result.current.table.nextPage()
    expect(navigate).toHaveBeenLastCalledWith({ pagina: 4 })
    result.current.table.setSorting([{ id: 'codigo', desc: false }])
    expect(navigate).toHaveBeenLastCalledWith({ orden: 'codigo', pagina: undefined })
    result.current.table.setGlobalFilter('beto')
    expect(navigate).toHaveBeenLastCalledWith({ q: 'beto', pagina: undefined })
  })
})
```

Run: FAIL.

- [ ] **Step 2: Implementar `features/url-state.ts`**

```ts
import type { SortingState, Updater } from '@tanstack/react-table'
import type { GridFeature } from '../types'

export type UrlSearch = { pagina?: number; orden?: string; q?: string }

export function ordenToSorting(orden: string | undefined): SortingState {
  if (!orden) return []
  const desc = orden.endsWith('-desc')
  return [{ id: desc ? orden.slice(0, -5) : orden, desc }]
}

export function sortingToOrden(sorting: SortingState): string | undefined {
  const first = sorting[0]
  if (!first) return undefined
  return first.desc ? `${first.id}-desc` : first.id
}

const resolve = <T,>(updater: Updater<T>, prev: T): T =>
  typeof updater === 'function' ? (updater as (old: T) => T)(prev) : updater

/**
 * Estado controlado desde la URL de la ruta: `pagina`, `orden` y `q`. Cambiar orden o búsqueda
 * vuelve a la primera página. La ruta valida la URL con los schemas de shared; el grid no valida.
 */
export function urlState(opts: {
  search: UrlSearch
  navigate: (patch: Record<string, unknown>) => void
  pageSize: number
}): GridFeature {
  const pagination = { pageIndex: Math.max((opts.search.pagina ?? 1) - 1, 0), pageSize: opts.pageSize }
  const sorting = ordenToSorting(opts.search.orden)
  const globalFilter = opts.search.q ?? ''
  return {
    id: 'urlState',
    tanstack: {},
    options: () => ({
      state: { pagination, sorting, globalFilter },
      onPaginationChange: (u: Updater<typeof pagination>) => {
        const next = resolve(u, pagination)
        opts.navigate({ pagina: next.pageIndex + 1 })
      },
      onSortingChange: (u: Updater<SortingState>) => {
        opts.navigate({ orden: sortingToOrden(resolve(u, sorting)), pagina: undefined })
      },
      onGlobalFilterChange: (u: Updater<string>) => {
        opts.navigate({ q: resolve(u, globalFilter) || undefined, pagina: undefined })
      },
    }),
  }
}
```

Nota: `urlState` no registra `rowSortingFeature` ni `rowPaginationFeature`: los aporta `sorting()`/`pagination()`; en modo servidor no hacen falta row models. Si la tabla no registra `filtering`, `globalFilteringFeature` debe aportarse aquí para que exista `setGlobalFilter`: añadir `tanstack: { globalFilteringFeature }` (import desde `@tanstack/react-table`).

Run: PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/data-grid
git commit -m "feat(web): estado del DataGrid controlado desde la URL (pagina, orden, q)

Refs #53"
```

---

### Task 18: Migrar trabajos (modo servidor, orden en URL, columna Código fija) + E2E

**Files:**
- Modify: `apps/web/src/features/cases/cases-table.tsx`, `cases-table.test.tsx`, `apps/web/src/routes/_app/trabajos/index.tsx`, `apps/web/src/features/cases/case-views.ts` (nada si `caseListQuerySchema` ya incluye `orden`)
- Modify: `apps/web/e2e/trabajos.spec.ts` (nuevo test `@clave`)

**Interfaces:**
- `CasesTable({ rows, total, hidePrices, search, onSearchChange })` — `search: Partial<CaseListQuery>`, `onSearchChange(patch)` = `updateSearch` de la ruta; `total` = `data.total`. La paginación se dibuja con `DataGrid.Pagination` (los botones Anterior/Siguiente de la ruta desaparecen). `CasesFilters` sigue igual (filtros de clínica, doctor, estado, fechas, técnico) y el buscador `q` también sigue en `CasesFilters` (ya tiene su `<label>`), así que `urlState` no mapea `q` aquí: pasar `search.q` solo si se quiere; para no duplicar el buscador, `filtering` **no** se registra en trabajos.

- [ ] **Step 1: Tests (RED)** — adaptar los 6 existentes a las props nuevas (añadir `total={rows.length} search={{}} onSearchChange={vi.fn()}`) y añadir:

```tsx
  it('ordenar por Entrega navega con orden=entrega y sin pagina', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    const onSearchChange = vi.fn()
    renderWithRouter(<CasesTable rows={rows} total={2} hidePrices={false} search={{ pagina: 2 }} onSearchChange={onSearchChange} />)
    await user.click(await screen.findByRole('button', { name: 'Ordenar por Entrega' }))
    expect(onSearchChange).toHaveBeenLastCalledWith({ orden: 'entrega', pagina: undefined })
  })

  it('la columna Código queda fija a la izquierda', async () => {
    setMatchMedia(true)
    renderWithRouter(<CasesTable rows={rows} total={2} hidePrices={false} search={{}} onSearchChange={vi.fn()} />)
    expect(await screen.findByRole('columnheader', { name: /Código/ })).toHaveStyle({ position: 'sticky' })
  })

  it('Siguiente navega a la página 2 cuando hay más filas en el servidor', async () => {
    setMatchMedia(true)
    const user = userEvent.setup()
    const onSearchChange = vi.fn()
    renderWithRouter(<CasesTable rows={rows} total={60} hidePrices={false} search={{}} onSearchChange={onSearchChange} />)
    await user.click(await screen.findByRole('button', { name: 'Siguiente' }))
    expect(onSearchChange).toHaveBeenLastCalledWith({ pagina: 2 })
  })
```

Run: FAIL.

- [ ] **Step 2: Reescribir `cases-table.tsx`**

Conservar `money`, `StatusIcon`, `DueCell`, `CodeCell` tal cual. Columnas con `defineColumns<CaseListRow>`:

- `codigo`: `col.accessor('code', { id: 'codigo', header: 'Código', cell: (c) => <CodeCell row={c.row.original} />, meta: { cellClassName: 'border-l-4', cellStyle: (r) => ({ borderLeftColor: STATUS_COLOR[(r as CaseListRow).status] }), width: 140 } })`
- `clinica`: accessor `(r) => \`${r.clinic.name} · ${r.doctor.name}\``, `id: 'clinica'`, header «Clínica / Doctor», cell con `truncate` + `title`, `meta: { cellClassName: 'max-w-[180px]' }`
- `paciente`: accessor `patientRef`, `enableSorting: false`, truncate
- `trabajo`: accessor `itemsSummary`, `enableSorting: false`, truncate
- `entrega`: accessor `(r) => r.promisedDate ?? r.dueDate`, `id: 'entrega'`, cell `<DueCell row=… today=… />`
- `estado`: accessor `status`, `id: 'estado'`, cell `<StatusChip>`
- `total` (solo si `!hidePrices`): `enableSorting: false`, `align: 'right'`, `font-mono`

`FEATURES` se construye con `useMemo` porque depende de `search`/`onSearchChange`: `[sorting(), pagination({ pageSize: CASE_PAGE_SIZE }), pinning({ left: ['codigo'] }), urlState({ search, navigate: onSearchChange, pageSize: CASE_PAGE_SIZE })]` (`CASE_PAGE_SIZE` de shared). `useDataGrid({ key: 'trabajos', columns, data: rows, features, mode: 'server', rowCount: total, getRowId })`. `Root` con `renderCard` = la tarjeta actual (mismo JSX del `renderMobile` de hoy) y `emptyMessage="No hay trabajos con estos filtros."`. Hijos: `<DataGrid.Content />` y `<DataGrid.Pagination />`.

Ojo: `onPaginationChange` de `urlState` recibe `{ pageIndex }`; `navigate({ pagina })` de la ruta hace `{ ...prev, ...patch, pagina: undefined }` en `updateSearch` — por eso `CasesTable` debe recibir dos callbacks o la ruta debe pasar un `onSearchChange` que **no** borre `pagina` cuando el patch la trae: cambiar `updateSearch` en la ruta a `navigate({ search: (prev) => ({ ...prev, pagina: undefined, ...patch }) })` (el patch manda). Test de ruta no existe; cubrir con el E2E.

- [ ] **Step 3: Ruta `trabajos/index.tsx`**

Quitar los botones Anterior/Siguiente y `hasMore`; pasar `total={data?.total ?? 0}`, `search={search}`, `onSearchChange={updateSearch}` a `CasesTable`; `query` incluye ya `orden` porque `search` viene de `parseCasesSearch` (schema con `orden`).

Run tests de `cases-table`: PASS. `pnpm test` completo: PASS.

- [ ] **Step 4: E2E `@clave`** en `trabajos.spec.ts` dentro de `describe('Trabajos')`:

```ts
  test('ordena por entrega desde la cabecera y el orden queda en la URL', { tag: '@clave' }, async ({ page }, testInfo) => {
    test.skip(testInfo.project.name !== 'escritorio', 'la cabecera de la tabla solo existe en escritorio')
    const { clinic, doctor } = await createClinicWithDoctor(page)
    const product = await createProduct(page)
    await createCase(page, { clinicId: clinic.id, doctorId: doctor.id, productId: product.id })
    await page.goto('/trabajos?vista=nuevos')
    await page.getByRole('button', { name: 'Ordenar por Entrega' }).click()
    await expect(page).toHaveURL(/orden=entrega/)
    await expect(page.getByRole('columnheader', { name: /Entrega/ })).toHaveAttribute('aria-sort', 'ascending')
    await page.getByRole('button', { name: 'Ordenar por Entrega' }).click()
    await expect(page).toHaveURL(/orden=entrega-desc/)
  })
```

Run: `pnpm e2e:pr` → verde (incluye el flujo esencial de trabajos sin cambios).

- [ ] **Step 5: Chrome (1280: columna Código fija con scroll interno de la tabla al estrechar; 390/360: tarjetas y paginación de 44 px) y commit**

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test
git add apps/web/src/features/cases apps/web/src/routes/_app/trabajos/index.tsx apps/web/e2e/trabajos.spec.ts
git commit -m "refactor(web): lista de trabajos sobre DataGrid en modo servidor con orden en la URL y código fijo

Refs #53"
```

---

### Task 19: Retirar `DataTable`, documentación y cierre

**Files:**
- Delete: `apps/web/src/components/data-table.tsx`, `apps/web/src/components/data-table.test.tsx`
- Modify: `docs/data-grid.md`, `CLAUDE.md` (regla 3), `docs/conventions.md` §5, `docs/architecture.md` §3.3 y ADR 15, `apps/web/e2e/accesibilidad.spec.ts`

- [ ] **Step 1: Borrar `DataTable` y comprobar que nadie lo importa**

```bash
git rm apps/web/src/components/data-table.tsx apps/web/src/components/data-table.test.tsx
grep -rn "components/data-table" apps/web/src && echo "QUEDAN IMPORTS" || echo "limpio"
pnpm typecheck
```

Expected: «limpio» y typecheck en verde.

- [ ] **Step 2: Accesibilidad E2E** — en `accesibilidad.spec.ts` añadir al test «configuración → usuarios: acciones de fila» y al de clínicas una aserción sobre la toolbar y la paginación en móvil:

```ts
    await expectTouchTargets(page, 'nav[aria-label="Paginación"] button, [role=search] input, [role=search] select')
```

(Solo si la pantalla tiene más de 25 filas la paginación existe; en clínicas el seed no llega, así que la aserción sobre `nav` puede no encontrar nodos: `expectTouchTargets` debe tolerar 0 coincidencias; si no, comprobar la implementación en `helpers.ts` y limitar a `[role=search] input`.) `pnpm e2e --project=escritorio --project=android` completo: verde.

- [ ] **Step 3: Docs**

- `docs/data-grid.md`: completar con `grouping`, `advancedFilter`, `resizing`, `pinning`, `urlState`, modo servidor (ejemplo de trabajos), persistencia y la regla de aislamiento; tabla «qué tabla usa qué feature» (spec §5).
- `CLAUDE.md` regla 3: añadir «Toda tabla usa `components/data-grid` (`DataGrid` + `useDataGrid` + `defineColumns`) y declara explícitamente sus features; `data-table.tsx` ya no existe. Guía: `docs/data-grid.md`.»
- `docs/conventions.md` §5: mismo texto, más «cada feature nueva del grid trae su test con y sin la feature; ninguna feature importa otra».
- `docs/architecture.md` §3.3: en «Componentes» sustituir «`DataTable` → futuro `DataGrid` #53» por «`DataGrid` (`components/data-grid/`, TanStack Table v9, features plug and play, `docs/data-grid.md`)»; ADR 15 → estado «vigente», motivo ampliado: «v9 estable con `tableFeatures()`; UI propia sobre shadcn (sin AG Grid/MRT); `orden` en la API de trabajos (decidido el 2026-09-12)».

- [ ] **Step 4: Verificación completa, commit y PR 3**

```bash
pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm e2e --project=escritorio --project=android
git add -A apps/web/src/components apps/web/e2e docs CLAUDE.md
git commit -m "docs: guía del DataGrid, regla de tablas y ADR 15 vigente; retirado data-table.tsx

Refs #53"
```

Revisión final de rama, ola de fixes y PR «DataGrid (3/3): trabajos en modo servidor, columnas fijas, URL y orden en la API» con `Closes #53`. Al mergear: #53 → Hecho, borrar el workspace SDD.

---

## Self-review (hecho al escribir el plan)

- **Cobertura de la spec**: §2 estructura (T1–T3, partes en T3/T11/T16), §2.1 contrato (T1; `transformData` añadido en T12 para el filtro avanzado), §2.2 columnas y meta (T2; `sortKey` sustituido por ids = claves de orden), §2.3 estado y persistencia (T10, T16, T17), §2.4 servidor (T3, T5, T18), §2.5 `orden` (T15), §3 features (T4–T6, T10–T12, T16–T17), §4 presentación y accesibilidad (partes en T3, aria en T4/T5/T10/T11, Chrome en cada tarea de UI), §5 migración (T7, T8, T13, T14, T18) y borrado (T19), §6 pruebas (por tarea + E2E T13/T18/T19), §7 docs (T9, T19), §8 criterios (T19 verifica; aislamiento en `data-grid.test.tsx` «sin features» y en cada test «sin la feature»).
- **Placeholders**: ninguno; donde una API de v9 podría diferir se indica qué confirmar con context7 y dónde anotarlo.
- **Consistencia de nombres**: `useDataGrid`, `defineColumns`, `DataGrid.Root/Toolbar/Content/Pagination`, `grid.has(id)`, `GridFeature.{id,tanstack,options,initialState,slots,transformData}`, factorías `sorting/pagination/filtering/advancedFilter/grouping/resizing/pinning/urlState`, claves `datagrid:<key>:sizing|pinning`, `orden` con sufijo `-desc`, ids de columnas de trabajos `codigo/clinica/entrega/estado`.
